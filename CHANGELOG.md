# CHANGELOG

- 2026-09-03: 프로젝트 작업환경을 신설함. `바이브코딩 기준`의 AGENTS.md/CLAUDE.md/THEME.md/PROMPT.md를 복사하고 프로젝트 적용값(원본 폴더 `web/`, 서버 `gunjin-campaign`/포트 8810)을 채움. 표준 테마(`web/styles/theme.css`)와 번들 폰트(`web/fonts/`), 캐시 무효화 미리보기 서버(`devserver.py`)를 적용하고 `.claude/launch.json`에 등록함.
- 2026-09-03: 사용자가 `source/`에 넣어둔 원본 자료(`body info.svg` — 신체 부위별 검진 항목·가격 그래픽, `background.eps` — 배경 디자인)를 확인함. SVG는 `web/assets/source/body-info.svg`로 복사해 랜딩 페이지(`web/index.html`)에 반영함. EPS는 이 환경에 Ghostscript/ImageMagick/Inkscape가 없어 변환하지 못해 미반영 상태로 남김.
- 2026-09-03: 캠페인 헤드카피·대상·기간 등 실제 문구가 아직 없어 `web/index.html`을 검진 항목 그래픽 + 자리표시자 카피로 구성한 1차 초안으로 둠.
- 2026-09-03: 작업 도중 `source/`에 `첨부2. '25년 건진 캠페인 홍보물.pdf`(2025년도 같은 캠페인 실제 홍보물)가 추가된 것을 확인함. 이 PDF의 구성(제목·기간·EVENT01/02·종합건진 프로그램 표·상담 연락처·추가검사 항목)을 그대로 옮겨 `web/index.html`을 다시 작성함. 추가검사 항목(SVG)은 2025년과 동일해 확정 반영했고, 기간·할인율·프로그램 가격·연락처는 2025년 값을 참고로만 옮기고 화면에 `2025 참고`/`2026 확인 필요` 라벨을 붙여 2026년 확정값 대기 상태로 표시함. `AGENTS.md`/`CLAUDE.md`에도 이 취급 규칙을 반영함.
- 2026-09-03: 사용자 요청("이미지로 된 텍스트를 수정가능한 폰트로 변경")에 따라, `source/body info.svg`(텍스트가 패스로 고정된 이미지)를 그대로 `<img>`로 얹었던 "추가검사 항목" 절을 걷어내고 같은 항목·가격을 `web/index.html`에 실제 HTML 텍스트(`.exam-grid`, 9개 계열 카드)로 다시 만듦. 이제 표준 폰트(Noto Sans KR)로 렌더링되는 선택·수정 가능한 텍스트다. SVG 사본은 원문 대조용으로만 `web/assets/source/`에 남겨둠.
- 2026-09-03: "02 추가검사 항목"을 편집 가능한 데이터베이스로 관리할 수 있도록 `data/추가검사항목.xlsx`를 신설함. 구분·항목명·가격(원)·비고 4개 열에 9개 계열 30개 항목을 정리했으며, `web/assets/source/body-info.svg`의 `<text>` 값과 동일한 소스다.
- 2026-09-03: 엑셀 → SVG 동기화 스크립트 `scripts/sync_exam_items.py`를 추가함. `web/assets/source/body-info.svg`와 `web/index.html`에 인라인된 같은 SVG의 각 `<text>`에 `id="exam-{엑셀행번호}-label/-label2/-price"`를 부여(엑셀 행 순서와 매핑, 2줄로 배치된 3개 항목만 `-label2` 추가)한 뒤, 스크립트가 엑셀 값을 읽어 두 SVG를 같은 값으로 갱신하도록 구현함. 사용자가 전달받은 엑셀을 직접 편집(일부 항목명에 공백 추가: "뇌CT"→"뇌 CT", "심장CT(조영제 포함)"→"심장CT (조영제 포함)" 등)한 것을 이 스크립트로 두 SVG에 반영해 검증함. 2줄 항목의 이름이 바뀌면 자동으로 1줄로 합치고 경고를 출력하도록 처리(자동 재줄바꿈은 지원하지 않음).
- 2026-09-03: 사용자 재요청("2번 페이지를 body.svg 디자인을 변경 없이 유지하고 텍스트 부분만 폰트로 변경")에 따라 위 HTML 카드 재구성안을 되돌리고, `source/body info.svg`의 **디자인은 그대로 유지한 채 텍스트만 교체**하는 방식으로 다시 작업함. 브라우저에서 `getBoundingClientRect`+`getScreenCTM` 역행렬로 9개 카테고리 박스 안 73개 텍스트 줄(제목·항목명·가격)의 정확한 좌표·정렬을 측정하고, 해당 글자 패스(`<path>`)만 제거한 뒤 같은 자리에 실제 `<text>`(Noto Sans KR, 원본과 동일한 색)를 넣은 사본을 만들어 `web/assets/source/body-info.svg`에 저장함(원본 421KB → 120KB). 신체 일러스트·박스 배경(`<rect>`)·장식 tick은 손대지 않음. `web/index.html`에는 이 사본을 인라인 `<svg>`로 삽입해 페이지 폰트를 상속받게 함(이전의 `.exam-grid` HTML 카드 방식은 제거).
- 2026-09-03: 사용자가 전달한 `M:\home\2026\0903 종합건진 캠페인\card design.svg`(Affinity Designer 산출물)를 기준으로 "02 추가검사 항목"의 카테고리 박스 디자인을 교체함. 파일을 `source/card design.svg`로 복사하고, 브라우저에서 카드 도형(`fill:white; stroke:rgb(152,233,255); stroke-width:5.28px`, 모서리 radius, 제목 색 `rgb(0,118,190)`, 제목 아래 구분선 `rgb(0,118,190)` 5px)의 정확한 색·비율을 측정함. 기존의 파랑/연파랑 두 톤 헤더 배지와 장식 tick 마크를 걷어내고, 9개 박스 모두 흰 배경+하늘색 테두리+그림자(SVG `feDropShadow`)+구분선으로 통일함. 항목·가격 텍스트의 폰트 크기·색은 요청대로 그대로 유지함. PET-CT 박스는 2줄 제목이라 구분선이 부제목과 겹쳐 아래로 30만큼 내려 고침. `web/assets/source/body-info.svg`와 `web/index.html` 인라인 사본 양쪽에 반영하고 미리보기로 렌더링·콘솔 에러 확인함.
- 2026-09-03: [참고 기사](https://techhub.iodigital.com/articles/particle-background-effect-with-canvas)를 바탕으로 "02 추가검사 항목" 섹션에 캔버스 파티클 배경 효과를 추가함. 배경색은 밝은 하늘색(`#e1f3ff`), 파티클·연결선은 밝은 회색(`rgb(200,200,200)`)으로 지정. `web/scripts/particles.js`를 신설했고(파티클 수 `(w+h)/50`, 연결 반경 `w/10+h/5`, 거리에 따라 선 투명도 감소, 경계 반사, `requestAnimationFrame` 루프), 섹션 컨테이너에 `<canvas class="exam-particles">`를 추가하고 카드·본문 요소는 `z-index`로 그 위에 올려 흰 카드 위에는 파티클이 비치지 않게 함.
  - 이 작업 중 `web/index.html`의 인라인 SVG가 `web/assets/source/body-info.svg`와 어긋나 박스가 잘못된 위치에 겹쳐 그려지는 문제를 발견함(이전 작업에서 divider 좌표 한 줄만 문자열 치환으로 반영하고 전체 재삽입을 하지 않은 게 원인). 인라인 `<svg>...</svg>` 블록 전체를 최신 `body-info.svg` 내용으로 통째로 교체해 수정하고, 두 파일의 카드/구분선/텍스트 개수를 대조해 완전히 일치함을 확인함. 재발 방지를 위해 `AGENTS.md`/`CLAUDE.md`에 "부분 치환 금지, 전체 블록 교체" 규칙을 추가함.
- 2026-09-03: 사용자가 강조 위치를 미리 표시해 전달한 `C:\Users\Mini\SynologyDrive\2026\0903 종합건진 캠페인\body-info.svg`를 참고해, 신체 일러스트에 6개 부위(뇌·심혈관·폐·갑상선·소화기·비뇨기) 강조 마커와 해당 카테고리 박스로 이어지는 연결선을 추가함. 참고 파일 끝부분에서 6개 좌표(`matrix(6.09419,...)` 변환의 원 마커)를 찾아 실제 렌더 좌표로 환산하고, Y값을 기준으로 뇌(머리 상단)·갑상선(목 하단)·심혈관/폐(가슴 좌우)·소화기(상복부)·비뇨기(하복부) 순으로 부위를 특정함. 마커는 사용자가 제시한 CSS 동심원(ripple) 애니메이션을 SVG `<style>`의 `@keyframes`로 옮겨 구현(반지름 13, 3초 주기, 0/1/2초 딜레이 3겹). 최초 파란색으로 만들었다가 사용자 요청으로 반투명 빨강(`rgba(230,0,0,0.8)`/`0.45`)으로 변경함. `web/assets/source/body-info.svg`와 `web/index.html` 인라인 사본 양쪽에 전체 블록 교체 방식으로 반영하고 카드/텍스트/마커 개수를 대조해 일치 확인.
- 2026-09-03: 사용자 요청으로 심혈관계·내분비계 박스를 맞바꿈(오른쪽 열 안에서 순서만 반전 — 실제 심장 위치에 심혈관계를 더 가깝게 둬서 강조선을 짧게 함). 두 박스의 높이 합+간격이 순서를 바꿔도 동일해 다른 박스는 옮기지 않고 카드·구분선·제목·항목(전부) y좌표만 델타 이동시킴. 이어서 근골격계·부인과도 맞바꿈(이번엔 열이 다른 교차 스왑이라 x,y 둘 다 델타 이동). 두 스왑 모두 강조 마커의 연결 대상이 아니라 마커 좌표는 변경하지 않음. 연결선 자체도 기존 직선에서 **수평 구간 → 대각선 구간**으로 꺾이는 폴리라인으로 바꾸고(꺾임점은 마커-박스 x거리의 중점), `stroke-linejoin:round`를 추가함. 매번 좌표 델타 계산 후 전용 검증 스크립트로 카드/헤더/항목 좌표가 기대값과 일치하는지 확인하고, `web/index.html` 인라인 SVG를 전체 블록 교체로 동기화함.
- 2026-09-03: 사용자가 `M:\...\2026\0903 종합건진 캠페인\body-info.svg`를 직접 수정해 6개 강조 마커 중 4개(심혈관·폐·소화기·비뇨기)의 위치/연결선 끝점을 다시 잡아 전달함. 애니메이션(`<style>`의 `@keyframes body-point-ripple`)은 그대로 두고 좌표만 반영: 심혈관 연결점 `1461.2,1189.9`→`1461.2,1090.0`, 폐 연결점 `992.1,1269.9`→`992.1,1290.0`, 소화기 연결점 `1461.2,1955.35`→`1461.2,1881.85`, 비뇨기 마커 자체를 `1234,1896`→`1234,1866`(참고 파일에서 `transform="matrix(1,0,0,1,0,-30)"`로 표현된 값)으로 이동하고 연결점도 `992.1,1808.1`→`992.1,1710.0`로 변경. `web/assets/source/body-info.svg`·`web/index.html` 양쪽에 전체 블록 교체로 반영.
- 2026-09-03: 왼쪽 열(뇌·폐·비뇨기계·부인과·PET-CT)과 오른쪽 열(내분비계·심혈관계·소화기계·근골격계)을 각각 상단(뇌/내분비계, `y302.6`)·하단(PET-CT/근골격계, `y3114.5`)에 맞추고, 그 사이 박스들은 높이 합을 뺀 나머지 공간을 간격 수만큼 균등 분배(왼쪽 간격 195.8, 오른쪽 간격 275.97)해 세로 간격을 통일함. 카드·구분선·제목·항목 좌표를 전부 재계산해 이동했고, 각 치환을 "정확히 1번 매치"로 검증하는 스크립트(`align_columns.py`)로 처리해 조용히 실패하는 일이 없게 함.
  - 사용자가 보고한 "부인과 가로줄(구분선) 삭제" 문제도 이번에 함께 처리함. 재정렬 과정에서 9개 구분선 전부를 명시적으로 다시 계산·치환하고 "정확히 1번 매치"를 강제해, 부인과를 포함한 모든 박스의 구분선이 새 위치에 정확히 존재하는지 확인함.
- 2026-09-03: 사용자가 스크린샷에 빨간 원으로 표시해 "부인과만 가로줄이 없다"고 재차 지적함. 좌표는 정확했지만(값 자체는 맞았음) DOM 순서 문제였음을 발견: 이전 스왑들이 좌표값만 문자열 치환으로 옮기고 요소 위치는 옮기지 않다 보니, 부인과의 구분선이 우연히 "부인과 카드보다 앞"에 놓인 슬롯의 값을 갖게 됐고, SVG는 그린 순서대로 위에 덮어그리므로 흰 카드가 구분선을 완전히 가려버렸음(부인과만 이 조합이 우연히 발생). ElementTree로 그 구분선 요소를 부인과 카드 바로 뒤로 옮겨(값은 그대로, 그리기 순서만 수정) 해결함. 이어서 나머지 8개 구분선도 "자신보다 뒤에 그려지는 카드와 겹치는지" 전수 검사해 추가로 가려진 것이 없음을 확인함.
- 2026-09-03: 사용자가 참고 파일을 다시 수정해 6개 강조선의 끝점을 각 대상 박스의 **세로 중간 지점**(edge 기준 수직 중앙)으로 옮겨 전달함. 좌표를 그대로 옮겨 반영: 뇌→`992.1,660.0`, 심혈관→`1461.2,1180.0`, 폐→`992.1,1360.0`, 갑상선→`1463.07,460.0`, 소화기→`1461.2,2080.0`, 비뇨기→`992.1,1860.0`(마커 자체 위치는 변경 없음). 재생성 후 카드 9/구분선 9/텍스트 73개 카운트가 그대로임을 확인해, 직전에 고친 부인과 구분선 그리기 순서 수정이 이번 갱신으로 되돌아가지 않았음을 검증함.
- 2026-09-03: 마지막 남은 2줄 항목 3개(뇌MRI&MRA+경동맥 MRA, 알츠하이머 위험도 혈액검사, 저선량 폐 MDCT)를 전부 1줄로 합치고 "간격 재정렬"함. `-label2` 텍스트를 삭제하고 `-label`에 합친 문구를 넣음(예: "· 뇌 MRI&MRA + 경동맥 MRA"). 뇌(머리) 박스는 6개 항목이 전부 1줄이 돼 높이 717.7→633.0으로, 폐 박스는 1개 항목만 남아 272.0→228.0으로 줄어듦(헤더→첫줄 106, 줄간격 81, 위/아래 여백 58/64 표준 적용). 왼쪽 열 전체(뇌·폐·비뇨기계·부인과·PET-CT)를 상단(`y302.6`)·하단(`y3114.5`) 정렬 유지한 채 간격을 다시 균등 분배(227.975)해 비뇨기계(`y1683.9`→`1619.6`)·부인과(`y2209.9`→`2177.7`)도 위로 당겨짐(PET-CT는 우연히 `y2786.3`으로 변화 없음). 뇌·폐·비뇨기계로 향하는 신체 강조선 끝점(박스 세로 중앙)도 새 박스 위치에 맞춰 재계산함. 텍스트 개수 73→70(라벨2 3개 삭제)으로 검증, `web/index.html` 전체 블록 교체로 동기화, 콘솔 에러 없음 확인.
- 2026-09-03: 사용자가 참고 파일에 섹션 제목·부제·안내문·장식 요소를 직접 그려 넣어 다시 전달하고, "svg 디자인 요소 반영하고 텍스트는 삭제"를 요청함(스크린샷에 기존 "02 추가검사 항목" 배지와 하단 개발용 안내 문단을 빨간 원으로 표시). 참고 파일 끝부분에서 제목 "추가검사항목"(최종 렌더 175px, 중심 1240.6,171.3), 영문 부제 "Extra examinations"(70px, 1238.7,312.7), 하단 안내문 "- 추가검사 사전예약 필수, 당일 검사 추가는 어렵습니다. -"(60px, 1242.4,3413.7), 장식 점 6개(반경10, `rgb(132,175,216)`)를 찾아 좌표를 getBoundingClientRect로 환산해 `<g id="pageTitleGroup">`/`<g id="decorDots">`로 추가함(원본은 글자별 tspan이었지만 `letter-spacing`을 준 단일 `<text>`로 단순화). 신체 일러스트도 참고 파일과 같이 세로로 185.5만큼 내림(`matrix(...,-190.224,385.181)`→`...,570.681`). 이 제목·안내문이 생기면서 중복되는 `web/index.html`의 "02" 번호 배지(`.section-badge`)와 개발용 안내 문단(`.exam-source-note`)을 삭제함. 카드 9/구분선 9 유지, 텍스트 70→73(제목·부제·안내문 3개 추가)으로 검증, 전체 블록 교체로 동기화, 렌더링·콘솔 에러 확인.
- 2026-09-03: 사용자가 "svg디자인과 동일한 구조로 반영, 인덱스 파일 위치가 틀어져 있다"고 지적함. 원인: 참고 파일의 `<g id="bodyPoints">`에 `transform="matrix(1,0,0,1,0,185.5)"`가 걸려 있는데(신체 일러스트를 내린 것과 같은 185.5만큼, 마커·연결선을 일러스트와 같이 움직이기 위한 그룹 트랜스폼), 이 트랜스폼을 놓치고 안쪽 좌표(마커 중심, 연결선 끝점)만 그대로 옮겨서 마커가 일러스트보다 185.5 위에 떠 있었음. 안쪽 좌표는 그대로 두고 `bodyPoints` 그룹에 같은 `transform`만 추가해 해결함(참고 파일과 구조 자체가 동일해짐). 재확인 결과 신체 부위 마커가 뇌·심장·폐·장기 위치에 정확히 겹치는 것으로 확인, `web/index.html` 전체 블록 교체로 동기화.
- 2026-09-03: 사용자 요청("css와 디자인 자료를 분리 모듈화")에 따라 `web/index.html`에 123,036자짜리 인라인 `<svg>...</svg>` 블록으로 통째로 박혀 있던 "02 추가검사 항목" 그래픽을 외부 파일 참조로 뽑아냄. `web/assets/source/body-info.svg` 맨 위 `<style>`에 `@font-face`(`Proposal Noto Sans KR`, 경로 `../../fonts/NotoSansKR-Regular.ttf`·`-Bold.ttf`, SVG 파일 위치 기준 상대경로)를 자체 추가해 이 SVG가 페이지의 폰트 상속 없이도 스스로 폰트를 로드하게 만든 뒤, `web/index.html`의 인라인 블록 전체를 `<object type="image/svg+xml" data="assets/source/body-info.svg" class="exam-object" aria-label="추가검사 항목 안내"></object>` 한 줄로 교체함(`index.html`이 약 127KB → 4,214바이트로 축소). `<img src>`는 이미지 컨텍스트라 SVG 내부 `@font-face`를 못 불러오므로 `<object>`를 사용함. `web/styles/app.css`도 정리: 이미 삭제된 `.section-badge`/`.exam-source-note` 셀렉터가 남아 있던 규칙을 `.exam-section .exam-svg-wrap{position:relative;z-index:1}`로 단순화하고, `.exam-svg-wrap svg{width:100%;height:auto;display:block}`/`.exam-source-note{...}`를 `.exam-object{width:100%;aspect-ratio:2481/3508;border:none;display:block}`로 교체함(`<object>`는 `<img>`처럼 `height:auto`로 자동 비율 조정이 안 돼 `aspect-ratio`로 SVG viewBox 비율을 명시).
  - 이 변경의 목적은 이번 세션에서 최소 3번(항목 줄바꿈 병합, PET-CT 구분선, 부인과 구분선) 반복 발생했던 "인라인 사본과 원본 파일이 어긋나는" 사고의 근본 원인 자체를 없애는 것임 — 사본이 하나뿐이므로 구조적으로 어긋날 방법이 없어짐. `AGENTS.md`/`CLAUDE.md`의 "전체 블록 교체 후 개수 대조" 규칙도 이제 불필요해져 각 문서에서 이 파일 하나만 고치면 된다는 설명으로 교체함.
  - 검증: `python devserver.py 8810 web` 미리보기에서 제목·부제·9개 카드·항목/가격 텍스트·구분선·신체 강조 마커(빨강 동심원 애니메이션)·연결선·파티클 배경·하단 안내문이 이전과 동일하게 렌더링됨을 스크린샷으로 확인. `obj.contentDocument`로 애니메이션이 실제로 진행 중임(`animationName: "body-point-ripple"`, 변화하는 `transform`)과 항목 텍스트의 계산된 `font-family`가 `"Proposal Noto Sans KR", "Noto Sans KR", sans-serif`로 올바르게 적용됨을 확인. 콘솔 에러 없음(과거 임시 미리보기 파일의 스테일 404만 있었고 실제 리소스와 무관), 네트워크 탭에서 `body-info.svg`가 `200 OK`로 로드됨을 확인.
- 2026-09-03: 이어서 사용자 요청("동심원 효과 별도 CSS로 저장, 배경 파티클 효과 별도 CSS로 저장")에 따라 남아 있던 두 군데 인라인/혼재 CSS를 각각 전용 파일로 분리함.
  - 동심원(ripple) 효과: `body-info.svg` 안의 `<style>`에 있던 `.body-point-line`/`.body-point-dot`/`.body-point-ripple`(`.r1/.r2/.r3`)/`@keyframes body-point-ripple`을 새 파일 `web/styles/body-points.css`로 옮기고, SVG 안의 `<style>`에는 `@font-face`만 남김. SVG는 `<object>`로 로드돼 자기 문서 컨텍스트를 가지므로, 파일 맨 위(루트 `<svg>` 앞)에 `<?xml-stylesheet href="../../styles/body-points.css" type="text/css"?>` 처리 명령을 추가해 외부 CSS를 불러오게 함. `<link>` 태그 대신 이 방식을 쓴 이유는 SVG 2의 `<link>` 브라우저 지원이 엇갈리는 반면, 독립 문서로 로드된 SVG의 `xml-stylesheet` 처리 명령은 크롬 계열에서 안정적으로 동작하기 때문.
  - 배경 파티클 효과: `web/styles/app.css`에 다른 일반 스타일과 섞여 있던 `.exam-section`(배경색 `#e1f3ff`·레이어링)·`.exam-section .exam-svg-wrap`(z-index)·`.exam-particles`(캔버스 스타일)를 새 파일 `web/styles/exam-particles.css`로 옮기고, `web/index.html` `<head>`에 `<link rel="stylesheet" href="styles/exam-particles.css">`를 추가함(`app.css`, `theme.css` 다음 순서).
  - 검증: 미리보기 새로고침 후 네트워크 탭에서 `styles/body-points.css`·`styles/exam-particles.css`가 각각 `200 OK`로 로드됨을 확인. `obj.contentDocument`에서 ripple 요소의 `getComputedStyle().animationName`이 여전히 `"body-point-ripple"`이고, 300ms 간격을 두고 두 번 측정한 `transform` 값이 서로 달라(`scale`이 변화) 애니메이션이 외부 스타일시트로도 정상 작동함을 확인. 스크린샷으로 카드·마커·연결선·배경·안내문 레이아웃이 이전과 동일함을 확인. 콘솔 에러는 기존과 동일하게 스테일 `ref2.svg` 404뿐, 새로 추가된 에러 없음.
- 2026-09-03: 사용자가 최신 참고 파일(`C:\Users\Mini\SynologyDrive\2026\0903 종합건진 캠페인\body-info.svg`)을 다시 첨부하며 "그룹과 포지션 변경 없이 반영, 현재는 박스위치가 첨부 SVG와 다른 위치에 표시되고 있어, 현재 그룹형식에 맞춰서 관련 자료를 구분해서 유지보수가 용이하도록 적용"을 요청함. 원인 분석: 제목("추가검사항목")을 추가한 2026-09-03 앞선 작업에서 신체 일러스트와 강조 마커 그룹만 185.5 내렸을 뿐, 카드·구분선·헤더·항목 텍스트는 이동을 놓쳐 예전 좌표에 남아 있었음 — 그 결과 첫 줄 카드(뇌·내분비계)가 부제("Extra examinations") 아래쪽과 겹치고, 마지막 카드와 하단 안내문 사이에는 불필요하게 큰 빈 공간이 생겨 있었음. 참고 파일의 좌표(카드·구분선·커넥터 라인은 자체 `transform="matrix(1,0,0,1,0,185.5)"` 래퍼로 이미 최종값을 만들고, 헤더·항목 텍스트는 그 최종값을 직접 좌표로 가짐)를 우리 SVG와 일일이 대조해, 카드 9개·구분선 9개·헤더 10개(PET-CT 2줄)·항목 텍스트 60개 전체에 +185.5를 적용해 참고 파일과 정확히 일치시킴. 신체 일러스트 transform도 x값을 `-190.224`→`-178.07`로 참고 파일과 맞춤(y=570.681은 원래도 일치).
  - 같은 작업에서 "관련 자료를 구분해서 유지보수 용이하게" 요청에 따라, `Artboard11` 바로 아래 평평하게 나열돼 있던 ~100개 요소(카드·구분선·헤더·항목, `pageTitleGroup`·`decorDots`·`defs`·`bodyPoints`는 제외)를 9개의 `<g id="category-{slug}" aria-label="{카테고리명}">`(brain/lung/urology/gynecology/petct/endocrine/cardio/digestive/msk)로 묶음. 각 카테고리 그룹 안에 그 카테고리의 장식 점(있는 경우)·카드 rect·구분선·헤더 텍스트·항목 텍스트가 전부 들어있어, 이후 박스 하나를 손볼 때 그룹 하나만 열면 되게 함. 이 작업 중 각 카테고리 헤더 자리에 원본 Illustrator/Affinity 내보내기의 잔재로 남아 있던 빈 `<g>` 셸(글자를 패스→`<text>`로 바꾸며 시각적 내용 없이 남은 껍데기, 9개 그룹에 걸쳐 하위 그룹 600개 이상)을 확인·제거함(재구성 시 옛 평면 자식 목록을 통째로 버리고 필요한 요소만 새 그룹에 옮겨 담는 방식이라 자동으로 빠짐).
  - 처음에는 신체 부위 마커(뇌·심혈관·폐·갑상선·소화기·비뇨기) 6개도 각자 카테고리 그룹 안에 넣었으나, 사용자가 스크린샷에 빨간 원으로 표시하며 "원형 점과 선을 각그룹의 표시 상단으로 변경"을 요청함. 처음엔 좌표(카드 세로 중앙 → 헤더 위치)를 옮기는 것으로 잘못 이해해 6개 커넥터 라인의 박스쪽 끝점을 헤더 y로 옮겼으나, 사용자가 "위치 이동 취소" 후 "상단 이동이라는건 레이어 단계를 올려서 표시단계를 변경하라는거야"라고 정정함 — 즉 좌표가 아니라 그리기 순서(z-order) 문제였음. 좌표는 원래의 박스 세로 중앙 값으로 되돌리고, 대신 6개 마커(연결선+동심원 그룹)를 각 카테고리 그룹 밖으로 다시 빼서 문서 맨 끝의 별도 그룹 `<g id="bodyPointMarkers" aria-label="신체 부위 강조 마커">`에 모음 — 이렇게 하면 마커가 항상 모든 카테고리 박스보다 나중에(위에) 그려진다(원래 `bodyPoints` 구조와 같은 그리기 순서를 유지하되, 이번엔 각 마커에 `data-category="category-brain"` 같은 속성을 남겨 소속 카테고리를 계속 추적할 수 있게 함).
  - 검증: `python3 scripts/sync_exam_items.py` 실행 결과 "변경 없음"으로 텍스트 내용이 그대로 보존됐음을 확인(좌표만 옮기고 항목명·가격은 건드리지 않았음이 증명됨) — 이 과정에서 `web/index.html`의 인라인 SVG를 갱신하려던 죽은 코드 경로(모듈화 이전 흔적, 지금은 `index.html`에 `<svg>` 태그 자체가 없어 실행하면 에러남)를 발견해 함께 제거함. 미리보기에서 스크린샷으로 카드 9개·헤더·항목·구분선·마커·연결선·파티클 배경·하단 안내문이 참고 파일과 같은 비율로 배치됨을 확인(제목-첫 카드 간격, 마지막 카드-안내문 간격 모두 자연스러워짐). `category-brain` 그룹의 카드 rect가 `y=488.1, height=633.0`(세로 중앙 804.6)이고 해당 마커 연결선이 정확히 `992.1,804.6`에서 끝남을 스크립트로 확인. 콘솔 에러는 기존 스테일 `ref2.svg` 404뿐, 새 에러 없음.
  - 뒤이어 사용자가 "`<circle cx="992.1" cy="805.5" r="10" fill="rgb(132,175,216)" />`의 레이어 순서가 각 박스의 뒤쪽에 있어서 가려져 있어, 레이어를 상단으로 변경해"라고 지적함. 원인: 카테고리 그룹 재구성 때 장식 점을 옛 문서 순서(전체 `decorDots` 그룹이 카드보다 먼저 그려지던 순서) 그대로 각 카테고리 그룹의 **첫** 자식으로 넣었는데, 이제는 같은 그룹 안의 카드 rect가 그 바로 다음에 그려지면서 불투명한 흰 카드가 장식 점을 덮어버렸음(그룹을 나누기 전에는 전체 decorDots 그룹이 전체 카드 묶음보다 먼저 그려져 다른 카드에 가려지지 않았으나, 그룹 재구성으로 "자기 카드"와 순서가 바뀐 것). 6개 장식 점(뇌·내분비계·폐·심혈관계·소화기계·비뇨기계 카테고리 그룹 안) 모두를 각자 그룹의 **마지막** 자식으로 옮겨 자기 카드보다 나중에(위에) 그려지도록 고침 — 좌표값은 전혀 건드리지 않고 문서 내 위치만 이동함. 미리보기에서 `category-brain` 등 각 그룹의 자식 순서를 스크립트로 확인해 `circle`이 마지막에 옴을 검증, 콘솔 에러 없음.
- 2026-09-03: 사용자 요청("현재 구성중인 각 페이지는 각각의 페이지로 A4 사이즈로 PDF로 변환해야 해 기능을 추가해")에 따라 인쇄/PDF 저장 시 페이지 분리 기능을 추가함. `web/index.html`의 콘텐츠를 두 개의 논리적 "페이지"로 나눔: `<div class="pdf-page">`로 감싼 1페이지(제목·기간, EVENT 2종, "01 종합건진 프로그램" 표, 상담 연락처)와 `<section class="card exam-section pdf-page">`인 2페이지("02 추가검사 항목" SVG, 원래도 별도 `<section>`이라 클래스만 추가). `web/styles/app.css`의 `@media print` 블록에 `.pdf-page{break-after:page}`(마지막 페이지는 `break-after:auto`로 예외 처리)와 `.card,.event-card,.program-card{break-inside:avoid}`(개별 카드가 페이지 경계에서 잘리지 않게)를 추가함. 기존 "🖨 인쇄 / PDF" 버튼(`window.print()`)이 그대로 이 새 페이지 분리 규칙을 적용받으므로 브라우저 인쇄 대화상자에서 "PDF로 저장"을 고르면 A4 2장으로 나뉜다.
  - "02 추가검사 항목" 페이지는 `body-info.svg`의 viewBox 비율(2481:3508 ≈ 0.707)이 A4 용지 비율(210:297 ≈ 0.707)과 정확히 같게 디자인돼 있어, `.exam-object{width:100%;aspect-ratio:2481/3508}`가 인쇄 여백을 뺀 폭에 맞춰 자연스럽게 A4 한 장 높이 안에 들어간다.
  - 검증: 화면 스크린샷으로 `.pdf-page` 래퍼가 레이아웃에 아무 영향을 주지 않음을 확인(기존 화면과 동일). `document.styleSheets`로 `@media print` 규칙이 문법 오류 없이 파싱돼 `.pdf-page`/`break-inside` 선언이 그대로 들어있음을 확인. 콘솔 에러는 기존 스테일 `ref2.svg` 404뿐. 이 브라우저 도구로는 실제 인쇄 미리보기(용지 분할 결과)까지는 확인할 수 없어, 1페이지 콘텐츠 분량이 많아지면(예: 종합건진 프로그램 항목이 늘어나는 경우) A4 한 장을 넘겨 의도치 않게 3페이지로 나뉠 수 있다 — 실제 인쇄 결과는 사용자가 브라우저에서 직접 확인 필요.
- 2026-09-03: 사용자가 "01 종합건진 프로그램" 표를 스크린샷에 빨간 원으로 표시하며 "엑셀데이터베이스 기반 자료 업데이트 구조로 변경"을 요청함. "02 추가검사 항목"이 이미 엑셀→스크립트 구조인 것과 같은 패턴을 적용하되, 이쪽은 SVG가 아니라 순수 HTML/CSS라 좌표 동기화 없이 훨씬 단순하게 구현함.
  - `data/종합건진프로그램.xlsx` 신설(프로그램명/구분/가격(만원)/정상가(만원)/비고, 13행 8프로그램). 같은 프로그램명을 가진 연속된 행이 카드 한 장의 여러 줄이 되는 구조(예: 건강증진(기본)의 "남"/"여" 두 행 → 카드 안 두 줄). `추가검사항목.xlsx`와 같은 서식(Arial, 헤더 볼드)으로 맞춤.
  - `scripts/sync_program_items.py` 신설: 엑셀을 프로그램명 기준으로 그룹핑해 `web/data/program.js`에 `window.PROGRAM_ITEMS = [...]`(JSON 배열)로 씀. `web/scripts/program.js` 신설: 그 배열을 읽어 `#programGrid`에 `.program-card`/`.p-name`/`.p-row`/`<b>`/`.strike` DOM을 직접 생성(라벨 없으면 앞 텍스트 생략, 정상가 없으면 취소선 생략 — 남성정밀·VIP건진처럼).
  - `web/index.html`의 하드코딩된 8개 `.program-card` 블록을 빈 `<div class="program-grid" id="programGrid"></div>`로 교체하고, `<script src="data/program.js">` → `<script src="scripts/program.js">`를 `particles.js` 앞에 추가(데이터 스크립트가 먼저 로드돼야 함).
  - 검증: `node --check`로 두 JS 파일 문법 확인. 미리보기 스크린샷으로 8개 카드가 기존과 완전히 동일하게 렌더링됨을 확인. 엑셀 값 하나(건강증진 남 40→41)를 임시로 바꾸고 스크립트를 재실행해 `program.js`와 화면에 반영됨을 확인한 뒤 다시 40으로 되돌려 재실행하는 왕복 테스트로 파이프라인 전체가 실제로 동작함을 검증. 콘솔 에러 없음.
- 2026-09-03: 사용자가 "전체 페이지 svg 파일로 출력"을 요청함(범위: 01+02 두 페이지 모두, 방식: 실제 벡터 SVG 재구성 — 사용자가 직접 선택). "02 추가검사 항목"은 이미 `body-info.svg`로 존재하므로, 이번 작업은 "01 종합건진 프로그램" 페이지를 `body-info.svg`와 같은 2481×3508 A4 캔버스 규격의 새 벡터 SVG로 만드는 것.
  - 측정 방법: 브라우저 뷰포트를 733px(A4 210mm에서 8mm 여백을 뺀 인쇄 폭)로 맞추고 topbar·placeholder-banner를 숨긴 뒤, `.pdf-page`(1페이지) 컨테이너 기준 상대좌표로 헤드라인·EVENT 카드·프로그램 카드·연락처 바의 모든 텍스트·박스를 `getBoundingClientRect`+`getComputedStyle`로 측정(이전 body-info.svg 제작 때 쓴 것과 같은 기법). 측정폭 685px(= 733 - 24px×2 콘텐츠 패딩)를 캔버스 여백(8mm≈94.5유닛)을 뺀 폭(2292유닛)에 맞추는 배율(×3.346)을 구해 모든 좌표·폰트 크기에 적용.
  - `web/assets/source/program-page.svg` 신설: 헤드라인(가운데 정렬 텍스트)·EVENT 01/02 카드(세로 그라데이션 배경)·"01 종합건진 프로그램" 섹션(흰 카드+그림자, 번호 배지, 8개 프로그램 카드)·연락처 바(대각선 그라데이션)를 전부 `<rect>`/`<text>`/`<linearGradient>`로 재구성. 정상가는 SVG `text-decoration="line-through"` + `fill-opacity="0.6"`로 취소선 재현. `body-info.svg`와 같은 `@font-face`(Proposal Noto Sans KR)를 자체적으로 가짐.
  - 버그 하나 발견·수정: 기간 줄("'26.??.?? – '26.??.??" + "2026 확인 필요" 뱃지)은 원본 CSS가 `text-align:center`로 [텍스트+뱃지]를 한 덩어리로 가운데 정렬하는데, 처음엔 텍스트만 따로 전체 폭 기준 가운데 정렬(`text-anchor="middle"`)해 뱃지와 겹쳤음. 뱃지의 실제 렌더 시작 위치(margin-left 6px 제외)에 텍스트를 `text-anchor="end"`로 맞춰 겹침을 없앰. 그 외 요소는 전부 실제 측정된 절대 좌표를 그대로 쓰므로 이런 재계산 문제가 없었음(왼쪽 정렬 요소는 자기 위치가 곧 정답이라 서식 로직을 재현할 필요가 없었음).
  - 검증: 생성한 SVG를 브라우저로 직접 열어 스크린샷 대조 — 헤드라인·EVENT 카드·8개 프로그램 카드·연락처 바가 실제 화면과 동일하게 배치됨을 확인, 기간 줄 겹침 수정 후 재확인. `text-decoration`/`fill-opacity`가 실제로 `line-through`/`0.6`으로 계산됨을 확인. 총 `<text>` 개수(64개)를 수작업 집계와 대조해 요소 누락·중복이 없음을 확인. `python3 -c "import xml.etree.ElementTree as ET; ET.parse(...)"`로 XML 유효성 확인, 콘솔 에러 없음.
  - 이 파일은 **스냅샷**이다 — `program.js`나 엑셀 데이터가 바뀌어도 자동으로 갱신되지 않으며, `web/index.html` 화면에도 연결돼 있지 않다(순수 전달용 산출물). 최신 상태가 다시 필요하면 같은 측정 절차를 재실행해야 한다.
- 2026-09-11: 사용자가 공식 확정 포스터(`M:\home\2026\0903 종합건진 캠페인\캠페인 포스터-outlined.pdf`, 이후 `C:\Users\Mini\SynologyDrive\2026\0903 종합건진 캠페인\`에서 같은 파일의 `.svg`도 전달 — 두 경로는 같은 Synology NAS를 가리키는 서로 다른 드라이브 문자임)를 전달하며 "캠페인 페이지를 첨부한 PDF와 페이지 순서를 기준으로 반영"을 요청함. PDF는 1페이지만 보였지만 SVG는 viewBox `0 0 10254 3508`로, 루트에 `Artboard1~4` 4개 아트보드가 나란히 배치돼 있었음(각 아트보드 자체는 body-info.svg와 동일한 2480.32×3507.87 A4 캔버스). 각 아트보드를 `viewBox`만 바꿔 격리해 브라우저로 확인한 결과, 성균관대학교·삼성창원병원(SCMC) 공동 명의의 4페이지 완전히 새로운 포스터 세트였음을 확인:
  - Artboard1(표지, 신규): "2026 검진캠페인" 타이틀(Jalnan2 폰트, 그라데이션+흰색 아웃라인), 확정 기간("2026.9.28(월)~2027.1.30(토)"), EVENT 01/02 홍보 문구(원문 정확한 버전 확보 — "VIP 건진, 추가 검사 항목은 할인 제외", "베타 아밀로이드 단백질 응집도를 확인 분석하는 혈액검사"), 삼성창원병원 미래관 건물 사진+설명, "Pride & Future" 태그라인.
  - Artboard2("캠페인 프로그램", 기존 "01"의 확장판): 건강증진코스에 "여성 에센셜" 신규 티어 추가(3열: 남성건진/여성 에센셜/여성건진), 장수건진·소화기 정밀·뇌정밀에도 각각 에센셜 옵션 추가(기존 2단 가격 구조 → 3단으로 확장), 상담 시 필요 정보 안내문 추가.
  - Artboard3("추가검사 항목", 기존 "02"와 거의 동일): 기존 body-info.svg와 카테고리·항목·가격 전부 동일(`id="exam-N-label"`/`-price"` 구조도 동일해 같은 계보로 보임). 로고 2종("Pride & Future" 태그라인, 성균관대·SCMC 로고)이 추가됐고 제목 폰트가 Jalnan2 스타일로 바뀌었으며 "Extra examinations" 영문 부제는 없음.
  - Artboard4("VIP 건진 패키지", 완전 신규): VIP1(330만원)/VIP2(540만원)/MVG(300만원) 각각 검사 항목 목록, "1박 2일 숙박" 명시, 다크 네이비+골드 톤 별도 디자인.
  - 사용자가 `source/Jalnan2/`(Jalnan2TTF.ttf, Jalnan2.otf)와 `source/Paperlogy-1.001/`(9개 굵기 ttf) 폰트 파일을 직접 프로젝트에 추가함 — 포스터에 쓰인 실제 폰트.
  - AskUserQuestion으로 반영 범위(4페이지 전부, 포스터 순서대로 표지→프로그램→추가검사→VIP)를 확인받은 뒤 작업 시작.
  - **아트보드 추출 방법 확립**: 각 `Artboard{N}`은 `transform="matrix(a,0,0,d,e,f)"`로 결합 캔버스에 배치돼 있고, 로컬 트림 영역은 모든 보드가 동일하게 `(425.197, 94.488, 2055.12, 3413.39)`(내부의 배경 rect로 확인) — 따라서 새 표준 viewBox는 `f"{a*425.197+e} {d*94.488+f} {a*2055.12} {d*3413.39}"`로 계산되며 항상 2480.32×3507.87 크기가 된다(Artboard1은 우연히 e,f가 거의 0으로 상쇄돼 "0 0 2480 3508"이 됨 — 이게 처음엔 일반 공식으로 착각할 뻔했으나 나머지 3개 보드로 검증하며 바로잡음). 추출 스크립트(`extract_board.py`, 프로젝트 밖 스크래치패드에 임시 작성)가 `<g id="Artboard{N}">` 서브트리를 통째로 가져오고, 그 서브트리가 참조하는 `<defs>` 항목을 **`xlink:href`뿐 아니라 모든 속성값의 `url(#id)` 패턴까지** 재귀 추적해 필요한 것만 새 `<defs>`에 담는다 — 처음엔 `xlink:href`만 추적해 VIP 페이지의 어두운 배경 패턴·금색 장식 테두리·검사 항목 설명 텍스트가 통째로 빠지는 사고가 났고(패턴/필터가 `fill="url(#...)"`처럼 CSS 스타일 참조라 xlink 스캔에 안 잡힘), 정규식 기반 범용 참조 스캐너로 고쳐 재추출해 해결함(VIP 페이지 defs 15개→61개로 늘어나며 정상화).
  - 4개 파일 신설: `web/assets/source/cover-page.svg`(표지, 1.9MB), `program-page.svg`(캠페인 프로그램, 935KB — 이전 턴에 손으로 재구성했던 같은 이름의 파일을 이 추출본으로 완전히 교체), `examitems-page.svg`(추가검사 항목, 238KB), `vip-page.svg`(VIP 패키지, 941KB). 공유 폰트 파일 `web/styles/poster-fonts.css` 신설(Jalnan2 4개 별칭 + Paperlogy 9개 굵기 + Proposal Noto Sans KR — 전부 `@font-face`), 4개 SVG 모두 `<?xml-stylesheet href="../../styles/poster-fonts.css" type="text/css"?>`로 공유 참조. 폰트 실물은 `web/fonts/Jalnan2/`, `web/fonts/Paperlogy/`로 복사.
  - `web/index.html`을 4개 `<section class="poster-page pdf-page"><object ...></section>`로 전면 재작성 — 기존 헤드라인/EVENT/프로그램 그리드/연락처 바 HTML과 관련 스크립트(`data/program.js`, `scripts/program.js`, `scripts/particles.js`)를 전부 제거하고 포스터 SVG 4장으로 교체. `placeholder-banner` 문구도 "1차 초안" 설명에서 "공식 포스터를 페이지 순서대로 반영했다"는 안내로 교체. `web/styles/app.css`에서 이제 안 쓰이는 `.card`/`.headline`/`.event-*`/`.section-badge`/`.program-*`/`.contact-bar`/`.exam-object`/`.exam-section` 등 다수 규칙을 제거하고 `.poster-page`/`.poster-object`(각 SVG를 `width:100%;aspect-ratio:2481/3508`로 꽉 채움, 풀블리드라 카드 배경·패딩 없음)로 교체. 인쇄 페이지 분리(`@media print`의 `.pdf-page{break-after:page}`)는 그대로 유지, 이제 4장 인쇄로 자연히 확장됨.
  - `scripts/sync_exam_items.py`의 대상을 `body-info.svg`→`examitems-page.svg`로 변경. 이 과정에서 실제 버그를 하나 발견·수정함: 새 SVG는 `id="exam-N-label"`이 `<text>`가 아니라 `<g>`에 붙어 있고(포스터 원본 구조), 또 원본 Affinity 커닝 때문에 일부 항목은 마지막 한두 글자가 별도 `<tspan>`(절대좌표)으로 잘려 있었음(예: "뇌 C" + `<tspan>T</tspan>`). 처음 고친 버전은 `<g>` 대응까지만 했고(`_resolve_text_targets` 추가) 비교는 `.text`만 봐서, 이미 최신인 항목들이 "다름"으로 오탐돼 tspan을 남긴 채 새 텍스트를 덧썼다(예: "· 뇌 CT" + 남은 `<tspan>T</tspan>` → 시각적으로 우연히 겹쳐 안 보였지만 구조적으로 중복). `_full_text()`(자손 텍스트까지 합쳐 비교)와 `_set_flat_text()`(갱신 시 옛 tspan을 지우고 통짜 텍스트로 씀) 헬퍼로 재수정. 엑셀 값을 임시로 바꾸고(뇌 CT→뇌 CT 정밀검사) 재실행→화면 확인(중복 없이 깨끗하게 반영됨)→원복→재실행(변경 없음 확인)까지 왕복 테스트로 검증.
  - 예전 파일(`web/assets/source/body-info.svg`, `data/종합건진프로그램.xlsx`, `scripts/sync_program_items.py`, `web/data/program.js`, `web/scripts/program.js`, `web/scripts/particles.js`, `web/styles/exam-particles.css`, `web/styles/body-points.css`)은 삭제하지 않고 남겨둠 — 더 이상 화면에 연결돼 있지 않은 참고용 흔적. [CLAUDE.md](CLAUDE.md)에 "이제 쓰이지 않는 예전 파일" 섹션으로 명시함.
  - 검증: 4개 추출 SVG를 각각 브라우저로 직접 열어 스크린샷 대조(로고·사진·그라데이션·장식 패턴·본문 텍스트 전부 원본과 동일하게 렌더링됨을 확인). `index.html`에서 4페이지가 순서대로(스크롤) 나타남을 확인, 콘솔 에러 없음. `sync_exam_items.py` 수정 후 왕복 테스트로 엑셀 편집 파이프라인이 새 구조에서도 안전하게 동작함을 확인.
- 2026-09-11: 사용자가 상단 안내 배너("사용자가 전달한 공식 포스터를... 반영했습니다")를 스크린샷에 빨간 원으로 표시하며 "표시 내용 삭제"를 요청함. `web/index.html`에서 `.placeholder-banner` 블록을 제거하고, `web/styles/app.css`의 관련 규칙(`.placeholder-banner`, `.placeholder-banner b`, `@media print`의 숨김 규칙)도 함께 정리함.
- 2026-09-11: 사용자가 예전 `web/scripts/particles.js` 경로를 다시 가리키며 "배경을 지우고 파티클 적용해"를 요청함. 예전에는 "02 추가검사항목" 섹션 하나에만 깔던 캔버스 파티클을, 이번에는 `<main class="content">` 전체(포스터 4페이지를 감싸는 컨테이너) 배경으로 확장 적용함: `<canvas class="exam-particles">`를 `.content`의 첫 자식으로 추가하고 `<script src="scripts/particles.js">`를 다시 불러옴. `web/styles/app.css`에 `body{background:#e1f3ff}`(theme.css의 장식 그라데이션을 덮어써 "배경을 지움")과 `.exam-particles{position:absolute;inset:0;z-index:0}`(`.content{position:relative}` 기준, `.content`의 실제 스크롤 높이만큼 캔버스가 늘어남 — `getBoundingClientRect()` 기반 sizing이라 별도 처리 없이 됨)를 추가하고, `.poster-page{position:relative;z-index:1}`로 각 포스터 페이지가 파티클보다 위에 불투명하게 그려지도록 함(페이지 사이 여백·좌우 여백에서만 파티클이 비쳐 보임). CSS는 예전 `exam-particles.css` 파일을 재사용하지 않고 `app.css`에 새로 씀(그 파일은 특정 섹션 전용으로 짜여 있어 페이지 전체 배경에는 맞지 않음). 검증: 스크린샷으로 배경 전체에 걸쳐 파티클이 흐르는 것을 확인, `canvas.width/height`가 `.content.scrollHeight`(약 5150px)와 정확히 일치함을 확인해 페이지 하단(VIP)까지 빠짐없이 덮임을 검증, 콘솔 에러 없음.
- 2026-09-11: 사용자가 스크린샷에 빨간 원으로 cover-page.svg의 EVENT 오퍼 문구("종합검진 20% 할인", "알츠하이머 검사 추가")와 program-page.svg의 가격 숫자("40만원"×2, "48만원")를 표시하며 "폰트 적용"을 요청함 — 전부 `Jalnan2` 볼드체로 디자인됐어야 할 자리에 대체 서체(제네릭 볼드 산세리프)가 그려지고 있었음.
  - 긴 진단 과정: 네트워크 탭에서 폰트 파일이 전부 200 OK로 로드됨을 확인, `getComputedStyle().fontFamily`도 `"Jalnan2, 'Jalnan 2'"`로 정확함을 확인, `font-display:block→swap` 변경도 무효, JS로 `el.style.fontFamily='Jalnan2'` 강제 지정도 무효, 문제의 `<text>`를 제거하면 글자가 통째로 사라짐(다른 숨은 요소가 대신 그려지는 게 아니라 이 텍스트 자체가 진짜로 잘못된 폰트로 그려지고 있음을 확인) — 결국 이 텍스트의 클론을 SVG 루트에 옮겨 붙였더니 **그 클론만 올바른 Jalnan2로 렌더링됨**을 발견해, 원인이 깊게 중첩된 조상 `<g>` 체인 어딘가에 있음을 좁힘.
  - 진짜 원인 발견: `grep`으로 저장된 `cover-page.svg`의 `<g id="Artboard1" transform=...>` 값을 직접 확인하니 `matrix(2.4138,0,0,2.05536,-1026.34,-194.208)`였는데, 원본 소스(`source/캠페인 포스터-outlined.svg`)의 진짜 `Artboard1` transform은 `matrix(1.2069,0,0,1.02768,-513.169,-97.1038)`(정확히 절반 스케일)였다. `viewBox`도 `0 0 4961 7016`(정상은 `0 0 2480.32 3507.87`, 역시 정확히 2배)로 같이 틀어져 있었음 — 즉 **저장된 파일 자체가 추출 단계에서 손상**돼 있었고, 우연히 가로세로가 같은 배율로 같이 틀어진 데다 A4 비율이 그대로 유지돼 전체적인 배치·크기는 정상으로 보여 스크린샷만으로는 전혀 알아챌 수 없었다(유일한 증상이 이 좌표계 서브픽셀 오차에 민감한 일부 웹폰트 텍스트의 폰트 폴백이었던 것으로 보임 — 정확한 브라우저 내부 메커니즘까지는 규명하지 못함). `examitems-page.svg`·`vip-page.svg`도 확인해보니 **완전히 동일한** 손상 값(`matrix(2.4138,0,0,2.05536,-1026.34,-194.208)`)이 들어 있어 3개 파일 모두 같은 사고의 산물이었음(program-page.svg는 또 다른 손상값). 원본 소스 파일의 mtime은 세션 내내 변하지 않았음을 확인해 "원본이 도중에 바뀐 것"은 아님을 배제했으나, 정확히 어느 단계에서 왜 이 값이 섞여 들어갔는지는(재추출을 반복하며 매번 사라졌기 때문에) 끝내 규명하지 못함.
  - 조치: `scripts/extract_board.py`(스크래치패드)로 4페이지를 전부 새로 추출하고 이번엔 저장 직후마다 `<g id="Artboard{N}">` transform을 원본과 즉시 대조해 정확히 일치함을 확인한 뒤 확정함. `examitems-page.svg`는 재추출로 동심원 클래스가 초기화되므로 `python scripts/add_ripple_markers.py`를 다시 실행해 복구. `python scripts/sync_exam_items.py`도 재실행해 "변경 없음"(항목 텍스트가 그대로 보존됨)을 확인.
  - 재발 방지: `CLAUDE.md`의 "포스터가 갱신되면..." 추출 절차에 "저장 직후 아트보드 transform을 원본과 대조" 검증 단계를 추가함 — 근본 원인은 못 찾았지만, 이 대조 한 번으로 같은 사고를 항상 감지·재시도할 수 있다.
  - 검증: 4페이지 전부 스크린샷 재확인(표지 EVENT 오퍼, 프로그램 페이지 3단 가격, 추가검사항목 동심원 애니메이션 — `getComputedStyle().transform`이 400ms 간격으로 바뀜을 확인, VIP 페이지 골드 숫자) 전부 정상. `exam-N` id 60개 그대로, 콘솔 에러 없음. 디버깅용으로 만든 `web/_font_test*.html`/`.svg` 임시 파일 전부 삭제.
- 2026-09-11: 사용자가 4페이지 각각의 스크린샷에서 페이지 사이 여백(포스터 콘텐츠 밖, 배경 파티클이 비쳐 보이는 영역)을 빨간 원으로 표시하며 "배경레이어인데 투명도를 25%로 변경"을 요청함. `web/styles/app.css`의 `.exam-particles`(캔버스 파티클 배경 레이어)에 `opacity:0.25`를 추가함. 검증: `getComputedStyle(canvas).opacity`가 `"0.25"`로 반영됨을 확인, 스크린샷으로 페이지 사이 여백의 파티클 선·점이 눈에 띄게 옅어짐을 확인, 콘솔 에러 없음.
  - 곧이어 사용자가 "파티클 투명도 복구"를 요청해 `opacity:0.25`를 제거하고 원래대로(불투명, `opacity:1`) 되돌림. `getComputedStyle(canvas).opacity`가 `"1"`로 돌아왔음을 확인, 콘솔 에러 없음.
- 2026-09-11: 사용자가 program-page.svg의 "종합건진 상담 예약: 055)233-6000" 안내를 빨간 원으로 표시하며 "모바일에서 클릭 시 전화로 연결되는 기능 추가"를 요청함. 이 문구는 포스터 SVG 안에 그림(텍스트/패스)으로 박혀 있어 `<object>` 특성상 직접 클릭할 수 없으므로, `web/index.html`의 program-page·vip-page 두 `.poster-page` 안에 각각 투명 `<a href="tel:0552336000" class="tel-overlay tel-overlay--program|--vip">`를 추가하고 `web/styles/app.css`에 `%` 단위 절대 위치(`.poster-object{aspect-ratio:2481/3508}` 기준이라 뷰포트 폭이 바뀌어도 정렬 유지)를 지정함. 좌표는 브라우저에서 안내 배너를 실제로 스크롤해 보이게 한 뒤 `getBoundingClientRect()`로 두 번 측정(스크롤 직후 좌표가 settle되지 않아 첫 측정이 어긋나는 걸 발견해 `scrollTo` 직후 짧은 대기 후 재측정하는 방식으로 바로잡음)해 얻음. 인쇄용 CSS에는 `.tel-overlay{display:none}`을 추가해 인쇄물에는 안 나오게 함.
  - 검증: 빨간 반투명 배경을 임시로 씌워 스크린샷으로 두 오버레이 모두 실제 안내 배너 위치에 정확히 겹치는 것을 확인, `href` 값이 두 곳 다 `"tel:0552336000"`인 것을 확인, 모바일 뷰포트(375px)에서도 레이아웃이 유지됨을 확인(사이트 전체가 `theme.css`의 `body{min-width:720px}` 때문에 원래도 좁은 화면에서 가로 스크롤이 필요한 구조라 이 부분은 이번 작업 범위 밖으로 남김), 콘솔 에러 없음.
- 2026-09-11: 사용자 요청("시놀로지 웹스테이션에 자료 탑재")에 따라 완성된 `web/` 폴더를 시놀로지 NAS의 Web Station에 배포함. RaiDrive로 매핑된 `M:\`(`net use M:`로 확인한 실제 대상은 `\\RaiDrive-Mini\Synology`)의 `M:\web\`가 기존 Web Station 문서 루트(이 프로젝트와 무관한 메인 사이트)이고, 그 안 `M:\web\apps\health-campaign-2026\`이 이 프로젝트 전용 배포 슬롯으로 이미 만들어져 있었음(9월 3일자 구버전 2페이지 구성이 남아 있는 상태였음 — apps/ 하위에 프로젝트별 폴더를 두는 건 이 NAS의 기존 관례).
  - 배포 방식: 미러링(옛 파일 삭제 후 새로 복사) — `assets`/`fonts`/`scripts`/`styles`/`index.html`을 대상 폴더에서 먼저 삭제한 뒤, 현재 `web/`의 `assets`·`data`·`fonts`·`scripts`·`styles`·`index.html`을 그대로 `cp -r`로 복사함(이 Git Bash 환경엔 `rsync`가 없어 `rm`+`cp` 조합을 씀).
  - 검증: 배포된 `assets/source/`에 포스터 4페이지 SVG(cover/program/examitems/vip-page.svg, 각 1.9MB/936KB/239KB/942KB)와 예전 참고용 `body-info.svg`(61KB)가 소스와 동일한 크기로 존재함을 `ls -la`로 확인, 배포된 `index.html`(1,728바이트)에 `poster-object` 참조가 정확히 4개 있음을 `grep -c`로 확인.
  - 미확인 사항: 이 환경에서는 NAS의 실제 Web Station 접속 URL(호스트명/포트)을 알 수 없어(RaiDrive는 파일 경로만 노출하고 웹 접속 정보는 노출하지 않음) 배포된 사이트가 브라우저에서 실제로 정상 동작하는지는 확인하지 못함 — 사용자가 직접 접속해 확인 필요. `CLAUDE.md`에 이 배포 위치와 재배포 절차를 문서화함.
- 2026-09-11: 사용자가 상단바 왼쪽 소개 문구("건강검진 홍보물 작업환경")를 빨간 원으로 표시하며 "문구 삭제"를 요청함. `web/index.html`에서 `.tag` div를 제거하고 `web/styles/app.css`의 `.topbar .tag` 규칙도 함께 정리함. 검증: 스크린샷으로 제목 아래 문구가 사라지고 레이아웃이 그대로 유지됨을 확인, 콘솔 에러 없음.
- 2026-09-11: 사용자가 상단바 "🖨 인쇄 / PDF" 버튼을 빨간 원으로 표시하며 "인쇄 기능 제거 PDF 파일 저장 기능 추가"를 요청함. 구현 방식을 AskUserQuestion으로 확인 — (a) 기존 `window.print()`를 그대로 쓰되 라벨·안내만 "PDF 저장"으로 바꾸는 안, (b) jsPDF+svg2pdf.js로 클릭 한 번에 실제 PDF 파일을 직접 생성·다운로드하는 안(커스텀 폰트 10종 임베딩, 그라데이션/패턴 벡터 재현 등 구현 난도·검증 부담 큼) 중 사용자가 (a, 권장안)를 선택함. `web/index.html`의 버튼을 `<button class="print-btn" onclick="window.print()" title="인쇄 대화상자가 열리면 대상(프린터)을 'PDF로 저장'으로 선택하세요">📄 PDF로 저장</button>`로 변경(내부 동작은 기존과 동일 — 이미 구현된 `@media print`의 `.pdf-page{break-after:page}` A4 페이지 분할을 그대로 활용하는 가장 안전한 방법). 검증: 버튼 라벨이 화면에 정상 반영됨을 스크린샷으로 확인, 콘솔 에러 없음.
- 2026-09-11: 위 두 변경(상단바 문구 삭제, PDF 저장 버튼)을 반영해 `M:\web\apps\health-campaign-2026\`을 재배포함. 기존 절차대로 대상 폴더의 `assets`·`data`·`fonts`·`scripts`·`styles`·`index.html`을 삭제 후 `web/`에서 다시 복사(`fonts` 폴더 자체는 RaiDrive에서 일시적으로 삭제 권한 오류가 나 빈 채로 남겨두고 내용만 새로 덮어씀 — 결과에는 영향 없음). 검증: 배포된 `index.html`에 `.tag` div가 없고 버튼이 `📄 PDF로 저장`으로 바뀌었음을 `grep`으로 확인, `poster-object` 4개·`fonts/` 하위 파일 12개(Jalnan2 1 + NotoSansKR 2 + Paperlogy 9)가 소스와 일치함을 확인.
- 2026-09-11: 사용자가 VIP 페이지 하단 빈 여백을 빨간 원으로 표시하며 "화면을 가리지 않는 전화 연결 플로팅 버튼 추가"를 요청하고, 이어서 "원형 이모티콘 형태"로 모양을, 마지막으로 "전화 아이콘 심플하게 흰색 라인 픽토그램으로"로 아이콘 스타일을 지정함. 기존 `.tel-overlay`(program-page·vip-page 두 곳의 전화번호 배너 위에만 있는 탭 영역)와 별개로, 스크롤 위치와 무관하게 화면 어디서나 누를 수 있는 보조 진입점을 추가함. `web/index.html`에 `<a href="tel:0552336000" class="call-fab" aria-label="..." title="...">`를 `<main>` 뒤·`<script>` 앞에 추가하고, 내부에는 이모지 대신 Feather 아이콘 계열의 단순 선(stroke) 전화기 픽토그램을 인라인 `<svg fill="none" stroke="#fff" stroke-width="2">`로 넣음(외부 아이콘 폰트/라이브러리 없이 SVG path 하나로 완결). `web/styles/app.css`에 `.call-fab{position:fixed;right:20px;bottom:20px;width:56px;height:56px;border-radius:50%;background:linear-gradient(135deg,#0e3e78,var(--blue))...}`(topbar와 같은 그라데이션)로 원형 배지 스타일을 줌. 콘텐츠를 가리지 않도록 우하단 구석에 작게 고정했고, `@media print`에 `.call-fab{display:none}`을 추가해 인쇄/PDF에는 안 나오게 함. 검증: 스크린샷으로 원형 버튼과 흰색 라인 아이콘이 페이지 최상단·최하단 스크롤 모두에서 같은 화면 위치에 떠 있고 포스터 콘텐츠를 가리지 않음을 확인, `href`가 `tel:0552336000`이고 크기가 56×56px임을 JS로 확인, 콘솔 에러 없음.
- 2026-09-11: 사용자가 표지 페이지 제목 부분을 빨간 원으로 표시하며 "페이지 로딩 순서를 첫번째 페이지 우선으로 하고 관련된 폰트만 먼저 읽어서 로딩 속도를 개선"을 요청함. 기존에는 4개 포스터 SVG(cover 1.9MB+program 936KB+examitems 239KB+vip 942KB, 총 4MB대)가 `<object data="...">`로 페이지 로드와 동시에 전부 병렬 요청돼, 표지가 다른 3페이지·그 폰트들과 네트워크를 다투며 늦게 뜨는 구조였음.
  - 각 페이지에서 실제 쓰는 폰트를 먼저 확인함(`grep`으로 `font-family:` 조사): cover는 Jalnan2·JalnanGothic·Paperlogy-4Regular/6SemiBold/7Bold, program은 Jalnan2·Paperlogy-7Bold, examitems는 Paperlogy-7Bold만, vip는 Paperlogy-4Regular·7Bold — `poster-fonts.css`에 선언된 12개 폰트 중 "Proposal Noto Sans KR"을 포함한 나머지는 4개 포스터 어디에도 실제로 쓰이지 않음. 브라우저는 `@font-face`가 선언만 돼 있어도 그 폰트를 실제로 쓰는 텍스트가 문서 안에 있을 때만 내려받으므로(표준 지연 로딩), 각 SVG는 애초에 자기가 쓰는 폰트만 받아간다 — 문제는 폰트 낭비가 아니라 **4개 SVG 파일 자체가 동시에 요청되는 것**이었음.
  - 조치: `web/index.html`에서 표지(`cover-page.svg`)만 기존대로 `<object data="...">`로 즉시 로딩하고, 나머지 3개는 `data` 대신 `data-src`에 경로를 옮겨 초기 로드를 막음(`.poster-object{aspect-ratio:2481/3508}`가 이미 크기를 예약해줘서 빈 자리로 인한 레이아웃 밀림은 없음). 새 파일 `web/scripts/lazy-poster.js`를 추가해, 표지의 `load` 이벤트가 끝나면 program→examitems→vip 순서로 "이전 `<object>`의 `load`가 끝나야 다음 걸 시작"하는 체인으로 하나씩 `data-src`를 `data`로 옮겨 순차 로딩함. 각 SVG가 로드되는 시점에야 그 문서 안 실제 쓰이는 폰트도 요청되므로, 이 순차 로딩 하나로 "첫 페이지 우선"과 "관련 폰트만 먼저"가 함께 해결됨.
  - 검증: `node --check`로 새 스크립트 문법 확인. 새로고침 후 JS로 4개 `<object>`의 `data` 속성이 전부 실제 파일 경로로 채워져 있음을 확인(로컬 서버라 순차 로딩이 매우 빨라 육안으로 단계별로 관찰하긴 어려웠으나 코드 경로 자체를 검토해 순서를 보장함을 확인), 표지·추가검사항목(동심원 마커 포함)·VIP 페이지를 스크롤하며 스크린샷으로 전부 정상 렌더링됨을 확인, 콘솔 에러 없음.
- 2026-09-11: 사용자가 VIP 페이지 화면에서 전화 플로팅 버튼을 빨간 원으로 표시하며 "모바일 페이지에서 볼 때 아이콘 크기가 작음 크기를 2배로 확대"를 요청함. `web/styles/app.css`에 `.call-fab svg{width:26px;height:26px}`(기존엔 `<svg>` 태그에 `width="26" height="26"`을 직접 박아뒀던 걸 CSS로 옮김 — 미디어쿼리로 반응형 제어하려면 필요)와 `@media(max-width:600px)` 분기를 처음 추가했으나, 모바일 프리셋(375px)으로 실제 확인해보니 `window.innerWidth`가 375가 아니라 720으로 나와 이 미디어쿼리가 전혀 걸리지 않는 것을 발견함 — 원인은 `theme.css`의 `body{min-width:720px}`가 레이아웃 뷰포트 자체를 720px로 넓혀버려서(이미 알려진 좁은 화면 가로 스크롤 문제, [CHANGELOG.md](CHANGELOG.md)의 2026-09-11 tel-overlay 항목에서도 언급) `max-width` 기준 미디어쿼리가 실제 모바일 기기에서도 절대 발동하지 않는 구조였기 때문. 화면 너비 대신 **입력 방식**으로 판별하도록 `@media (hover:none) and (pointer:coarse)`(터치스크린 여부)로 바꿔 문제를 해결함 — 이 조건은 `min-width` 확대와 무관하게 실제 기기 종류만 본다. `.call-fab`을 112×112px, 아이콘을 52×52px로(둘 다 정확히 2배) 키움. 검증: 모바일 프리셋(375×812, 터치 에뮬레이션 포함)에서 `getComputedStyle`로 버튼 112px·아이콘 52px·`matchMedia('(hover:none) and (pointer:coarse)').matches === true`를 확인, 데스크톱 프리셋에서는 그대로 56px·26px로 남아있음을 확인, 콘솔 에러 없음.
- 2026-09-11: 사용자가 "PDF로 저장" 버튼을 다시 가리키며 "현재 디자인 기준으로 PDF파일 생성해서 저장, PDF로 저장은 실시간 생성이 아니라 생성된 PDF 파일을 다운로드 하는 기능으로 변경"을 요청함 — 이전 턴에 확인했던 "브라우저 인쇄창 유지" 방식(`window.print()`)을 폐기하고, 미리 만들어 둔 정적 PDF 파일을 직접 다운로드하는 방식으로 바꾸는 것.
  - PDF 생성 도구 조사: 이 환경엔 Ghostscript/Inkscape/LibreOffice(`soffice`)가 없고 `cairosvg`/`svglib`도 없어 SVG→PDF 직접 변환이 불가능함. 대신 Windows에 설치된 Edge(`msedge.exe`, Chromium 기반)를 헤드리스로 띄워 **화면에 실제로 보이는 그대로**(폰트·그라데이션·패턴·사진 전부 브라우저가 렌더링한 결과)를 PDF로 뽑는 방식을 택함 — jsPDF+svg2pdf.js로 클라이언트에서 재구성하는 것보다 훨씬 안전(폰트 임베딩·벡터 재현 문제 없음).
  - `pip install playwright`(Python 패키지만, 브라우저는 별도로 안 받고 `channel="msedge"`로 시스템에 이미 설치된 Edge를 그대로 씀)로 헤드리스 브라우저 제어 확보. 새 스크립트 `scripts/generate_poster_pdf.py`: `http://localhost:8810/`을 열고, `lazy-poster.js`의 순차 로딩이 끝나길(4개 `poster-object` 전부 `data` 채워짐) 기다린 뒤 `page.emulate_media('print')`로 기존 `@media print` CSS(`.pdf-page{break-after:page}` 등)를 그대로 적용해 A4 PDF를 생성함. Chrome의 print-to-pdf 결과가 중복 스트림이 많아 24MB로 컸는데, `pymupdf`로 무손실 재압축(`garbage=4, deflate=True, clean=True`)해 8MB대로 줄임(페이지 렌더 비교로 화질·내용 손실 없음을 확인).
  - `web/assets/2026-gunjin-campaign.pdf`(8.2MB, 4페이지)로 저장하고, `web/index.html`의 버튼을 `<button onclick="window.print()">`에서 `<a class="pdf-btn" href="assets/2026-gunjin-campaign.pdf" download="2026년도_건진_캠페인.pdf">📄 PDF로 저장</a>`로 교체함(클래스명도 이제 인쇄가 아니므로 `print-btn`→`pdf-btn`으로 바꾸고 `web/styles/app.css`에 동일하게 반영, `text-decoration:none` 추가로 `<a>`가 `<button>`과 똑같이 보이게 함).
  - 검증: 생성된 PDF의 4페이지를 각각 PNG로 렌더링해 스크린샷으로 대조 — 표지 사진·그라데이션, 프로그램 페이지 3단 가격표, 추가검사항목 동심원 마커(정지 프레임), VIP 골드 디자인 전부 화면과 동일하게 재현됨을 확인. `fetch(a.href,{method:'HEAD'})`로 200 OK·`content-type: application/pdf`·정확한 파일 크기를 확인, `download` 속성이 한글 파일명으로 올바르게 지정됨을 확인, 콘솔 에러 없음. **포스터 내용이 바뀌면 이 PDF가 저절로 갱신되지 않으므로 `scripts/generate_poster_pdf.py`를 다시 실행해야 함**을 `CLAUDE.md`에 명시함.
- 2026-09-11: 사용자가 `source/캠페인 포스터 라이트-outlined.svg`("라이트" 버전, 사용자가 표지·프로그램 페이지를 수정)를 첨부하며 "1,2 페이지를 수정했어 디자인 반영하고 폰트도 적용해"를 요청함. 이 파일을 `source/`에 복사한 뒤 `Artboard1~4` transform을 예전 `캠페인 포스터-outlined.svg`와 대조 — 전부 동일(같은 문서에서 파생됐고 표지·프로그램 페이지 내용만 갱신됨을 확인). 실제 쓰는 폰트도 조사(`grep font-family:`) — Jalnan2·JalnanGothic·Paperlogy-4/6/7 5종으로 기존과 동일해 새 폰트 파일 추가는 필요 없었고, 기존 절차(재추출 시 `poster-fonts.css` xml-stylesheet 자동 포함)로 폰트 적용이 저절로 됨.
  - Artboard1→`cover-page.svg`, Artboard2→`program-page.svg`만 이 새 소스로 재추출함(Artboard3·4는 이 파일의 수정 대상이 아니므로 그대로 둠). 기존 defs 개수(9개·15개)와 정확히 일치해 추출 로직이 일관됨을 확인, 추출 직후 저장된 두 파일의 Artboard transform을 원본과 대조해 정확히 일치함을 확인(2026-09-11 앞선 사고 재발 방지 절차 그대로 적용).
  - 반영: 표지 타이틀이 "2026 검진캠페인"(2줄)에서 "2026 종합건진 추천캠페인"(3줄)으로 바뀌었고, 프로그램 페이지는 "여성 에센셜" 티어명이 "여성 라이트"로 바뀜.
  - 검증 중 한 차례 혼선: 미리보기 탭에서 표지 사진(삼성창원병원 미래관)이 사라진 것처럼 보였으나, `fetch(url,{cache:'no-store'})`로 서버가 실제로 주는 파일을 직접 확인하니 사진이 정상적으로 들어있었음 — 오래 떠 있던 탭이 `<object>` 리소스를 캐시해서 예전 내용을 계속 보여준 것이었고, `object.data`에 캐시 무효화 쿼리(`?t=...`)를 붙여 강제로 다시 로드하니 사진이 정상 표시됨을 확인. 실제 파일 손실은 없었음 — 재발 방지로 `CLAUDE.md`에 이 캐시 주의사항을 추가함.
  - 사용자가 이어서 프로그램 페이지의 "장수건진, 소화기 정밀, 뇌정밀 코스에도 에센셜 코스가 추가되었습니다" 캡션을 빨간 원으로 표시하며 "라이트로 수정"을 지적함 — 티어명은 전부 "라이트"로 개명됐는데 이 캡션 한 곳만 "에센셜"이 남아있던 원본 자체의 누락. 이건 디자인 변경이 아니라 순수 텍스트 오탈자이므로 이 환경에서 직접 고침: 파이썬으로 "에센셜" 문자열이 정확히 1번만 나오는 것을 확인한 뒤 "라이트"로 치환.
  - `scripts/generate_poster_pdf.py`로 정적 다운로드 PDF도 재생성함(7.9MB, 4페이지 — 새 표지·프로그램 반영, PNG 렌더로 대조 확인).
  - `M:\web\apps\health-campaign-2026\`에 `assets`·`styles`·`index.html` 재배포함(`assets/source/cover-page.svg`·`program-page.svg`·`2026-gunjin-campaign.pdf` 갱신분 포함).
- 2026-09-14: 사용자가 "결과물을 깃허브에 업로드해서 호스팅해"를 요청함. 프로젝트 루트에 git 저장소가 없어(`git status` → not a git repository) 새로 만드는 작업이었음.
  - GitHub CLI(`gh`)가 이 환경에 없어 `winget install GitHub.cli`로 설치함. 인증은 device flow(`gh auth login --web`)로 진행 — 코드(예: `3B58-24BC`)와 `https://github.com/login/device` URL을 사용자에게 전달하고, 계정 로그인·코드 입력은 사용자가 직접 본인 브라우저에서 완료함(비밀번호·토큰을 이 환경이 직접 다루지 않음). 로그인 계정: `chunrock`.
  - 저장소 구조 결정: GitHub Pages는 브랜치의 루트 또는 `/docs` 경로만 서빙 가능하고 임의의 하위 폴더(`web/`)는 못 쓰므로, NAS 배포와 같은 "미러 복사" 패턴을 재사용해 `docs/`를 `web/`의 사본으로 만들고 Pages 소스를 `main`브랜치 `/docs`로 지정함(빌드 파이프라인 없이 정적 파일 그대로 서빙, 가장 단순한 방법).
  - `git init -b main`으로 프로젝트 루트 전체(소스 원본 `source/`, 작업 스크립트 `scripts/`·`data/`, 문서 `CLAUDE.md`/`AGENTS.md`/`CHANGELOG.md`/`THEME.md`/`PROMPT.md`, 배포 사본 `web/`·`docs/`)를 커밋함(총 102MB, 100MB 초과 파일 없음을 사전 확인). 첫 `git add -A` 때 의도치 않게 딸려 들어온 것 2건을 발견해 제외함: 사용자가 별도로 만든 백업 zip(`web/26년도 건진 캠페인.zip`, web/과 내용 중복)과 Python 실행 중 자동 생성된 `scripts/__pycache__/*.pyc`. 둘 다 `.gitignore`에 등록하고 `git rm --cached`로 인덱스에서 뺀 뒤(아직 push 전이라 `git commit --amend`로 안전하게 정리) 커밋을 다시 확정함.
  - `gh repo create 2026campaign --public ...`을 시도했으나 "Name already exists" 에러 — 확인해보니 사용자가 하루 전(2026-09-13)에 이미 `chunrock/2026campaign`(설명 "삼성창원병원 건진캠페인", Private, 빈 저장소)을 만들어 둔 상태였음. 그 저장소에 `git remote add origin` + `git push -u origin main`으로 그대로 올림.
  - GitHub Pages는 무료 요금제에서 Private 저장소를 지원하지 않아(`gh api .../pages` POST 시 422 "Your current plan does not support GitHub Pages for this repository") 활성화가 막혀 있었음. 사용자에게 확인 후("저장소를 Public으로 전환" 선택) `gh repo edit --visibility public --accept-visibility-change-consequences`로 공개 전환하고 Pages를 `main`/`/docs`로 활성화함(`gh api -X POST repos/chunrock/2026campaign/pages`).
  - 검증: `gh api repos/.../pages/builds/latest`로 빌드 완료(`"status":"built"`, 약 44초 소요) 확인. 실제 URL `https://chunrock.github.io/2026campaign/`을 브라우저로 열어 표지·VIP 페이지(스크롤 양끝)가 정상 렌더링됨을 확인, `fetch`로 PDF 다운로드 링크가 200·`application/pdf`로 응답함을 확인, 콘솔 에러 없음.
- 2026-09-14: 사용자가 "스마트폰에서 화면을 볼 때 로딩 후 페이지 전체가 한번에 보였으면 좋겠어. 현재는 확대된 화면으로 보이고 있어"를 요청함. 모바일 프리셋(375×812)으로 재현해보니 실제로 포스터가 잘린 채 확대된 것처럼 보이고 가로 스크롤이 필요했음(원인은 앞서 여러 번 언급된 `theme.css`의 `body{min-width:720px}` — 좁은 화면에서도 레이아웃 뷰포트를 720px로 강제로 넓혀버려 `window.innerWidth`가 375가 아니라 720으로 나오던 그 문제, 처음으로 화면 전체 배치에 영향을 주는 형태로 사용자에게 직접 보고됨).
  - `web/styles/app.css`의 기존 터치기기 전용 미디어쿼리(`@media (hover:none) and (pointer:coarse)`, 이전에 전화 버튼 2배 확대에 썼던 것과 동일 — `max-width` 기준은 이 사이트에서 안 통하므로)에 `body{min-width:0}`을 추가해 모바일에서만 그 제약을 없앰. 데스크톱(마우스 포인터)에는 영향 없음.
  - 검증: 모바일 프리셋에서 재로딩 후 JS로 `window.innerWidth`가 375(정상)로, `document.body.scrollWidth`도 375(가로 스크롤 없음)로 확인. 스크린샷으로 표지 전체(로고·타이틀·EVENT 박스·사진·캡션)와 프로그램 페이지 시작부까지 화면 하나에 잘림 없이 들어옴을 확인, 전화 버튼도 우하단 고정 위치(20px 마진) 그대로임을 확인. 데스크톱에서는 `body`의 `min-width`가 여전히 720px로 유지됨을 확인(터치 기기에만 적용되는 스코프 확인), 콘솔 에러 없음.
  - `docs/`(GitHub Pages)와 `M:\web\apps\health-campaign-2026\`(시놀로지) 양쪽에 `styles/app.css` 재배포, GitHub에는 커밋 후 push.
- 2026-09-14: 방금 모바일 뷰포트 수정(위 항목) 직후, 사용자가 "자동 확대하면서 전화 아이콘이 너무 커졌어 절반 사이즈로 축소해"를 지적함 — `min-width:720px` 버그가 있을 때는 레이아웃이 720px로 눌려 있어 2배(112px) 아이콘도 화면상 작아 보였지만, 뷰포트 수정으로 실제 375px 폭을 그대로 쓰게 되자 같은 112px가 화면의 30%를 차지할 만큼 커져 보였음. `web/styles/app.css`의 터치기기 미디어쿼리에서 `.call-fab`/`.call-fab svg` 크기 재정의(112px/52px)를 제거함 — 결과적으로 기본값(56px/26px, 데스크톱과 동일)으로 돌아가 정확히 절반이 됨. 검증: `getComputedStyle`로 56px/26px 확인, 모바일 스크린샷으로 버튼이 더 이상 과도하게 크지 않음을 확인, 콘솔 에러 없음. `docs/`(GitHub Pages)·NAS 재배포.
