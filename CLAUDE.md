# CLAUDE.md

이 폴더는 2026년도 건강검진(건진) 캠페인 홍보물을 웹 자료로 만드는 작업환경이다.
Claude Code, Codex 등 어떤 도구로 작업하든 [AGENTS.md](AGENTS.md)를 우선 적용한다.

## 프로젝트 목적

`source/캠페인 포스터-outlined.pdf`(및 같은 이름의 `.svg`)가 **공식 확정 디자인**이다. 성균관대학교·삼성창원병원(SCMC) 공동 명의의 4페이지 포스터 세트로, 표지·캠페인 프로그램·추가검사 항목·VIP 건진 패키지 순서로 구성된다. `web/index.html`은 이 4페이지를 같은 순서로 그대로 보여주는 랜딩 페이지다. 2026-09-11부터 이 포스터가 이전의 "2025년 PDF 참고값 + 자리표시자" 1차 초안을 완전히 대체했다 — 화면의 문구·가격·기간은 전부 포스터 원본 확정값이며 더 이상 `2025 참고`/`2026 확인 필요` 라벨이 없다.

## 핵심 파일

- 원본(읽기 전용): `source/캠페인 포스터-outlined.pdf`·`.svg`·`.afdesign`(공식 확정 디자인, 4개 아트보드 — Artboard1=표지, Artboard2=캠페인 프로그램, Artboard3=추가검사 항목(내부에 `Artboard11`이라는 하위 그룹명이 있는데 이건 Affinity 내부 레이어 이름일 뿐 다른 페이지가 아님), Artboard4=VIP 건진 패키지), `source/Jalnan2/`·`source/Paperlogy-1.001/`(포스터에 쓰인 폰트 — 사용자 제공). **2026-09-11(2차)부터 표지·캠페인 프로그램(Artboard1·2)은 `source/캠페인 포스터 라이트-outlined.svg`("라이트" 버전)가 최신 원본이다** — 아트보드 구조·transform·viewBox는 기존 파일과 동일(같은 문서에서 파생), 표지·프로그램 페이지 내용만 갱신됨(표지 타이틀 "2026 검진캠페인"→"2026 종합건진 추천캠페인", 프로그램 페이지 "여성 에센셜"→"여성 라이트" 티어명 등). 추가검사 항목·VIP(Artboard3·4)는 이 파일이 갱신 대상이 아니므로 여전히 예전 `캠페인 포스터-outlined.svg` 기준.
- 웹 원본: `web/` (랜딩 `index.html`)
- **포스터 4페이지 SVG**(`web/assets/source/`): `cover-page.svg`(표지) · `program-page.svg`(캠페인 프로그램) · `examitems-page.svg`(추가검사 항목) · `vip-page.svg`(VIP 건진 패키지). 전부 포스터 원본 SVG에서 해당 아트보드 하나씩을 `scripts`(비정형, 1회성으로 실행한 추출 스크립트 — §"원본 자료 취급" 참고)로 뽑아낸 것으로, 로고·사진·그라데이션·폰트까지 원본 그대로다. `web/index.html`이 각각을 `<object type="image/svg+xml">`로 외부 참조한다(4개 모두 viewBox `... 2480.32 3507.87`인 A4 캔버스 — x 시작값만 원본 포스터 안에서 그 아트보드가 있던 위치라 페이지마다 다르다. 개별 파일로는 아무 의미 없는 숫자이니 신경 쓰지 않아도 된다).
- 공유 폰트 스타일시트: `web/styles/poster-fonts.css`(Jalnan2, Paperlogy 9종 굵기, Proposal Noto Sans KR의 `@font-face` 전부) — 4개 포스터 SVG가 전부 이 파일을 `<?xml-stylesheet href="../../styles/poster-fonts.css" type="text/css"?>` 처리 명령으로 공유 참조한다(`<object>`로 로드되는 SVG는 자기 문서 컨텍스트를 가지므로 가능). 폰트 실물은 `web/fonts/Jalnan2/`, `web/fonts/Paperlogy/`.
- 편집용 데이터베이스(엑셀): `data/추가검사항목.xlsx` — 구분/항목명/가격(원)/비고. "02 추가검사 항목" 페이지에서만 이 방식으로 계속 편집 가능(아래 참고).
- 동기화 스크립트: `scripts/sync_exam_items.py` — 엑셀 값을 `web/assets/source/examitems-page.svg`의 `id="exam-N-label"`/`-label2`/`-price` 요소(포스터 원본 구조상 id는 `<g>`에 붙어 있고 실제 텍스트는 그 안의 `<text>` 자손에 있다 — 스크립트가 둘 다 처리함)에 반영한다(엑셀 → SVG 한 방향).
- PDF 페이지 분리: `web/index.html`은 4개 포스터 페이지를 전부 `<section class="poster-page pdf-page">`로 감싼다. `web/styles/app.css`의 `@media print`에서 `.pdf-page{break-after:page}`(마지막은 예외)로 A4 용지 한 장당 페이지 하나가 나오게 한다 — 브라우저 인쇄가 아니라 아래 "PDF 다운로드"가 이 CSS를 그대로 활용한다.
- **PDF 다운로드**: 2026-09-11부터 상단바 "📄 PDF로 저장" 버튼은 `window.print()`(인쇄 대화상자)가 아니라 **미리 만들어 둔 정적 파일**을 직접 내려받는 `<a class="pdf-btn" href="assets/2026-gunjin-campaign.pdf" download="2026년도_건진_캠페인.pdf">`다. 이 PDF(`web/assets/2026-gunjin-campaign.pdf`, 약 8MB, 4페이지)는 `scripts/generate_poster_pdf.py`로 생성한다 — Playwright(Python)로 설치된 Edge(`channel="msedge"`, 별도 브라우저 다운로드 없이 시스템 Edge를 그대로 씀)를 headless로 띄워 `http://localhost:8810/`을 열고, `lazy-poster.js`의 순차 로딩이 끝나기를(4개 `poster-object` 전부 `data` 채워짐) 기다린 뒤 `@media print` CSS 그대로(`page.emulate_media('print')`) A4로 인쇄해 PDF를 뽑고, `pymupdf`로 무손실 재압축(`garbage=4, deflate=True`)해 용량을 24MB→8MB로 줄인다. **포스터 디자인·내용이 바뀌면(재추출, 엑셀 동기화, 레이아웃 CSS 수정 등) 화면만 갱신되고 이 PDF는 저절로 안 바뀌므로, `python devserver.py 8810 web`을 띄운 채 `python scripts/generate_poster_pdf.py`를 다시 실행해 PDF도 같이 갱신해야 한다** — 실행 전 `pip install playwright`(1회)가 필요하다.
- 배경 효과: `web/scripts/particles.js`(로직, 2026-09-11 재사용 — 예전 "02" 섹션 전용이었다가 지금은 `<main class="content">` 전체 배경으로 씀) — `<canvas class="exam-particles">`가 `.content`의 첫 자식으로 있고, `.content{position:relative}` 기준 `position:absolute;inset:0`로 페이지 전체 높이를 덮는다. `body{background:#e1f3ff}`(app.css, theme.css의 장식 그라데이션을 덮어씀)로 밝은 하늘색 배경을 깔고 그 위에 밝은 회색 입자+연결선이 흐른다. 포스터 페이지(`.poster-page`)는 `z-index:1`이라 각 페이지 자체는 불투명하게 위에 그려지고, 페이지 사이 여백·좌우 마진에서만 파티클이 비쳐 보인다. CSS는 별도 파일 없이 `app.css`에 직접 있다(예전 `exam-particles.css`는 이제 안 씀).
- 신체 부위 강조 마커의 동심원(ripple) 효과: 2026-09-11에 `examitems-page.svg`에 다시 살림. 포스터 원본은 마커 6곳(뇌·심혈관·폐·갑상선·소화기·비뇨기)마다 정적인 빨강 원 4개(투명도 .45×3 + .8×1, 각자 `<g transform=...>`로 한 겹씩 감싸진 채 4개가 연달아 형제로 있음)를 이미 갖고 있었다 — 애니메이션 CSS만 안 걸려 있었을 뿐. 그 4개를 문서 순서대로 `class="body-point-ripple r1/r2/r3"` + `class="body-point-dot"`로 바꾸고(기존 `style` 속성은 제거 — 클래스가 `fill`도 함께 정의함), 파일 맨 위에 `<?xml-stylesheet href="../../styles/body-points.css" type="text/css"?>`를 (기존 `poster-fonts.css` 참조에 이어) 추가로 넣어서 기존 `web/styles/body-points.css`(2026-09-03에 만든 파일, 그대로 재사용)의 `@keyframes body-point-ripple`을 불러온다. `<object>`로 로드되는 SVG는 `<?xml-stylesheet?>`를 여러 개 넣어도 전부 적용된다. **포스터를 다시 추출하면(아래 "포스터가 갱신되면..." 절차) 이 클래스 변환은 사라진다** — 원본 소스에는 클래스가 없으므로, 재추출할 때마다 `python scripts/add_ripple_markers.py`를 다시 실행해야 한다.
- 전화 연결(tel:) 오버레이: 2026-09-11에 추가. "종합건진 상담 예약: 055)233-6000" 안내가 program-page.svg·vip-page.svg 둘 다에 그림(포스터 안 텍스트/패스)으로 박혀 있어 그 자체는 클릭할 수 없다 — `<object>`는 내부 클릭을 페이지로 전달하지 않는다. 그래서 `web/index.html`의 해당 `.poster-page` 안에 투명 `<a href="tel:0552336000" class="tel-overlay tel-overlay--program|--vip">`를 겹쳐 놓고, `web/styles/app.css`에서 `%` 단위로 그 안내 문구 위치에 맞춰 배치했다(`.poster-object{aspect-ratio:2481/3508}`라 뷰포트 폭이 달라져도 `%` 배치가 그대로 맞는다). 좌표는 브라우저에서 실제 렌더 위치를 `getBoundingClientRect()`로 측정해 얻었다 — **포스터를 다시 추출해 이 안내 배너의 위치가 바뀌면 `.tel-overlay--program`/`--vip`의 `%` 값도 다시 측정해 고쳐야 한다.**
- 전화 연결 플로팅 버튼: 2026-09-11에 추가. 위 tel-overlay는 program-page·vip-page 두 곳의 안내 배너 위에서만 눌리므로, 스크롤 위치와 무관하게 항상 누를 수 있도록 `web/index.html` 맨 아래(`</main>` 뒤)에 원형 `<a href="tel:0552336000" class="call-fab">`를 추가함(내부는 흰색 선 픽토그램 인라인 SVG, 크기는 `<svg>`의 `width`/`height` 속성이 아니라 `web/styles/app.css`의 `.call-fab svg{...}`로 제어 — 미디어쿼리로 반응형 조정하려면 CSS 쪽에 있어야 함). `web/styles/app.css`의 `.call-fab{position:fixed;right:20px;bottom:20px;...}`로 화면 우하단에 고정, `@media print`에서는 숨김. 모바일에서는 `@media (hover:none) and (pointer:coarse){.call-fab{...2배...}}`로 버튼·아이콘을 2배 키운다 — **일반적인 `max-width` 기준 미디어쿼리는 이 사이트에서 안 통한다.** `theme.css`의 `body{min-width:720px}` 때문에 좁은 화면에서도 레이아웃 뷰포트가 720px로 강제로 넓어져(`window.innerWidth`가 375가 아니라 720으로 나옴) `max-width:600px` 같은 조건이 실제 모바일 기기에서도 절대 발동하지 않는다. 화면 너비가 아니라 터치 입력 여부(`hover:none`/`pointer:coarse`)로 판별해야 한다.
- 포스터 페이지 지연 로딩(로딩 속도 개선): 2026-09-11에 추가. 4개 포스터 SVG(총 4MB대)와 그 폰트가 페이지 로드 시 전부 한꺼번에 병렬로 요청되면 첫 페이지(표지)가 뜨는 속도가 늦어진다. `web/index.html`에서 표지(`cover-page.svg`)만 `<object data="...">`로 즉시 로딩하고, 나머지 3개(`program-page.svg`/`examitems-page.svg`/`vip-page.svg`)는 `data` 대신 `data-src`에 경로를 넣어 초기 로드를 막아둔다(`.poster-object{aspect-ratio:2481/3508}`가 이미 크기를 잡아줘서 레이아웃이 밀리지 않는다). `web/scripts/lazy-poster.js`(신규)가 표지의 `load` 이벤트 이후 program→examitems→vip 순서로 하나씩(이전 `<object>`의 `load`가 끝나야 다음 걸 시작) `data-src` 값을 `data`로 옮겨 순차 로딩한다. 각 SVG는 `<object>`로 로드될 때 자기 문서 안에서 실제 쓰는 `@font-face`만 그 시점에 요청하므로(브라우저의 표준 지연 폰트 로딩 — `poster-fonts.css`에 12개 폰트가 전부 선언돼 있어도 그 문서가 실제로 쓰는 폰트만 받는다), 이 순차 로딩 하나로 "첫 페이지 우선 + 관련 폰트만 먼저" 두 가지가 함께 해결된다.
- 디자인 기준: [THEME.md](THEME.md), `web/styles/theme.css` — 다만 포스터 페이지 자체는 포스터 고유 디자인(진한 파랑/네이비/골드)을 그대로 쓰고 THEME.md 팔레트를 따르지 않는다. THEME.md는 topbar 등 사이트 UI 크롬에만 적용된다.
- 미리보기: `python devserver.py 8810 web` — 서버는 프로젝트당 1개만 띄운다
- **실제 배포(시놀로지 Web Station)**: `M:\web\apps\health-campaign-2026\`(= NAS의 `web` 공유 폴더 안 `apps/health-campaign-2026/`, RaiDrive로 `M:`에 매핑돼 있음)가 배포 대상 폴더다. `web/`의 `assets`·`data`·`fonts`·`scripts`·`styles`·`index.html`을 그대로 미러링한 사본이며, 원본이 아니므로 이 폴더 안에서 직접 고치지 않는다 — 항상 `web/`을 고친 뒤 다시 복사(미러링, 옛 파일은 지우고 새로 복사)한다. 2026-09-11에 이 폴더를 처음 만든 뒤(Sep 3, 옛 2페이지 버전) 오늘 작업분(포스터 4페이지 전체)으로 갱신함. NAS의 실제 접속 URL(호스트/포트)은 이 환경에서 알 수 없으므로, 배포 후 실제 접속 확인은 사용자가 직접 한다.
- 변경 이력: [CHANGELOG.md](CHANGELOG.md)

### 이제 쓰이지 않는 예전 파일 (삭제하지 않고 남겨둠)

2026-09-11 포스터 전체 반영 이전에 만든 1차 초안의 흔적. 화면에는 더 이상 연결돼 있지 않지만, 참고용으로 지우지 않았다.

- `web/assets/source/body-info.svg` — "추가검사 항목"의 예전 버전(로고·태그라인 없음). `examitems-page.svg`로 대체됨.
- `data/종합건진프로그램.xlsx`, `scripts/sync_program_items.py`, `web/data/program.js`, `web/scripts/program.js` — "01 종합건진 프로그램"을 HTML 카드 그리드로 직접 그리던 예전 방식(2단 가격 구조). 포스터의 "캠페인 프로그램" 페이지(3단 가격 구조, 여성 에센셜 티어 포함)가 이를 대체했다.
- `web/styles/exam-particles.css` — 예전 "02" 섹션 전용 파티클 배경 CSS. `particles.js`는 2026-09-11부터 다시 쓰이지만(핵심 파일 "배경 효과" 참고), CSS는 이제 `app.css`에 직접 있고 이 파일은 안 쓴다.

## 작업 기준

- 웹 자료 수정은 `web/` 원본에만 한다. `source/`의 원본 파일은 고치지 않는다.
- **포스터 디자인 자체(레이아웃·색·사진·로고·타이포)를 바꾸는 일은 이 환경에서 하지 않는다.** 원본이 `source/캠페인 포스터-outlined.afdesign`(Affinity Designer)이므로, 디자인 변경은 사용자가 Affinity에서 하고 새 `.svg`/`.pdf`를 내보내면 그걸 다시 반영한다. 이 환경에서 할 수 있는 건 (a) 추가검사 항목의 항목명·가격을 엑셀로 편집, (b) 포스터가 갱신되면 4개 SVG를 다시 추출, (c) `index.html`/CSS 쪽 레이아웃(페이지 순서, 인쇄 분할 등) 조정이다.
- 추가검사 항목명·가격을 바꿀 때는 SVG를 직접 고치지 않고 **`data/추가검사항목.xlsx`를 먼저 고친 뒤 `python scripts/sync_exam_items.py`를 실행**한다. 이 스크립트가 `examitems-page.svg`의 텍스트를 갱신한다 — 디자인(일러스트·박스·로고·색)은 건드리지 않는다.
  - 이 텍스트는 원본 Affinity 커닝 때문에 마지막 한두 글자가 별도 `<tspan>`(절대좌표)으로 잘려 있는 항목이 있다(예: "뇌 CT"의 "T"만 따로). 스크립트는 비교할 때 텍스트 전체(`.text` + 자손 `<tspan>` 텍스트)를 합쳐서 보고, 실제로 바뀐 항목만 그 `<tspan>`을 지우고 통짜 텍스트로 다시 쓴다 — 값이 같으면 원본 tspan 구조를 그대로 둬서 커닝을 보존한다. **`<text>.text`만 보거나 tspan을 남긴 채 텍스트를 덧쓰면 안 된다** — 그러면 옛 tspan이 새 텍스트 위에 어긋나게 겹쳐 보이거나(운 좋게 안 보일 수도 있음) 잘못된 글자가 남는다. 이 버그가 실제로 한 번 있었고(2026-09-11) `_full_text`/`_set_flat_text` 헬퍼로 고쳤다.
  - 2줄로 나뉜 항목이 생기면(`-label2`) 자동 재배치를 못 하므로 SVG에서 수동으로 줄바꿈 위치를 조정해야 한다. 2026-09-11 기준 `-label2`가 붙은 항목은 없다.
  - 항목을 추가·삭제하는 구조적 변경은 이 스크립트로 할 수 없다(`id="exam-N-..."` 매핑을 새로 만들어야 함).
- **포스터가 갱신되면(사용자가 새 `.svg`를 `source/`에 올리면) 4개 페이지를 다시 추출해야 한다.** 방법: 새 `source/캠페인 포스터-outlined.svg`를 열어 루트의 `Artboard1~4` 각각의 `transform="matrix(a,0,0,d,e,f)"`을 읽고, 그 아트보드의 로컬 트림 영역(보통 `(425.197, 94.488, 2055.12, 3413.39)` — 값이 다르면 그 아트보드 안의 배경 rect를 찾아 확인)을 `viewBox = "{a*425.197+e} {d*94.488+f} {a*2055.12} {d*3413.39}"`로 계산해 새 viewBox로 쓰고, 그 `<g id="Artboard{N}">` 서브트리와 **그 서브트리가 실제로 참조하는 `<defs>` 항목만**(`xlink:href="#id"`뿐 아니라 `fill="url(#id)"`·`clip-path="url(#id)"`·`filter="url(#id)"` 등 모든 `url(#id)` 참조를 재귀적으로 추적 — 하나라도 놓치면 배경 패턴·그림자·장식이 빠진 채 렌더링된다) 뽑아 새 파일로 저장하고, `<?xml-stylesheet href="../../styles/poster-fonts.css" type="text/css"?>`를 xml 선언 바로 뒤에 넣는다. Artboard1→cover-page.svg, Artboard2→program-page.svg, Artboard3→examitems-page.svg, Artboard4→vip-page.svg 매핑이다. examitems-page.svg는 추출 후 `python scripts/add_ripple_markers.py`도 다시 돌려야 한다(위 "동심원 효과" 항목 참고).
  - **추출 직후 반드시 검증한다: 저장된 파일의 `<g id="Artboard{N}">` transform이 원본 소스의 같은 아트보드 transform과 정확히 같은지 대조한다**(`grep -o 'id="Artboard{N}" transform="[^"]*"' 파일`로 양쪽을 비교). 2026-09-11에 원인 불명의 사고로 저장된 파일 3개(cover/examitems/vip)의 이 transform이 실제 값(`matrix(1.2069,...)`류)이 아니라 `matrix(2.4138,0,0,2.05536,-1026.34,-194.208)`(examitems·vip는 이 값이 서로 완전히 동일했음 — 같은 값이 다른 아트보드에 잘못 들어간 정황)로 바뀌어 저장된 적이 있다. viewBox도 같이 틀어져서(예: `0 0 4961 7016`, 정상은 `0 0 2480.32 3507.87`) 전체적인 위치·크기는 우연히 A4 비율 그대로라 스크린샷만 봐서는 **전혀 티가 안 났고**, 유일한 증상이 포스터 안 일부 `Jalnan2`/`Paperlogy` 실제 텍스트(제목처럼 패스로 고정된 게 아니라 진짜 `<text>`인 것들 — EVENT 오퍼 문구, 가격 숫자 등)가 폰트를 무시하고 임의의 대체 서체로 렌더링되는 것뿐이었다(폰트 파일 자체는 정상 로드됨, `font-family` 계산값도 정확했음 — 오직 실제 페인트만 틀렸다). 재추출 후 다시 나타나면, 원인을 더 파기보다 그냥 다시 추출을 재시도하면(같은 스크립트를 다시 돌리면) 매번 사라졌다 — 근본 원인은 못 찾았지만 재현 가능한 회피법은 "저장 직후 transform 대조 → 틀리면 재추출"이다.
  - **미리보기로 검증할 때 브라우저 캐시를 주의한다.** 오래 떠 있던 탭에서 파일을 바꾼 뒤 단순히 같은 URL로 다시 `navigate`만 하면, `<object>`가 참조하는 SVG 리소스 자체는 예전 캐시된 내용을 계속 보여줄 수 있다(2026-09-11에 실제로 이걸로 "사진이 없어졌다"고 잘못 판단할 뻔함 — `fetch(url,{cache:'no-store'})`로 서버가 실제로 새 파일을 주는지 먼저 확인하거나, `object.removeAttribute('data')` 후 `object.data = url+'?t='+Date.now()`처럼 캐시 무효화 쿼리를 붙여 강제로 다시 로드해서 확인해야 한다.)
  - 포스터 소스가 여러 파일로 나뉠 수 있다(예: 특정 페이지만 다시 편집한 "라이트" 버전처럼 페이지 일부만 담당하는 파일). 이 경우 그 페이지들만 해당 파일에서 재추출하고, 나머지 페이지는 기존 소스 기준을 유지한다 — 위 "원본(읽기 전용)" 항목에 현재 어느 페이지가 어느 파일에서 왔는지 최신 상태로 적어둔다.
  - 재추출한 텍스트에 원본 리네이밍이 덜 반영된 오탈자가 남아있을 수 있다(예: 2026-09-11에 "여성 에센셜"→"여성 라이트"로 개명했는데 본문 캡션 한 곳엔 "에센셜"이 그대로 남아있었음). 사용자가 특정 문구를 지적하며 고치라고 하면, 디자인을 바꾸는 게 아니라 순수 텍스트 오탈자 수정이므로 이 환경에서 바로 `<text>` 내용을 고쳐도 된다(파이썬으로 정확히 몇 번 나오는지 세어 확인 후 치환 — 엉뚱한 곳까지 바뀌지 않게).
- 폰트가 늘어나면(새 굵기·새 패밀리) `web/fonts/`에 파일을 추가하고 `web/styles/poster-fonts.css`에 `@font-face`를 추가한다. 개별 SVG에는 손대지 않아도 된다(전부 같은 파일을 공유 참조).
- `web/assets/source/examitems-page.svg`(또는 다른 포스터 SVG)를 고치면 그걸로 끝이다. `web/index.html`은 `<object>`로 외부 참조만 하므로 별도의 "인라인 사본"에 다시 반영하는 단계가 없다. 저장 후 바로 미리보기 새로고침으로 확인한다.
- JS 수정 후 `node --check`, 화면 변경 후 미리보기 콘솔 에러 확인을 거친 뒤 완료를 보고한다.
