# AGENTS.md — 26년도 건진 캠페인 관련

## 프로젝트 적용값

| 항목 | 값 |
|---|---|
| 프로젝트명 | 26년도 건진 캠페인 관련 |
| 프로젝트 유형 | `web-static` |
| 웹 원본 폴더 | `web/` |
| 등록 서버 이름 | `gunjin-campaign` |
| 등록 서버 포트 | `8810` |
| 미리보기 실행 | `python devserver.py 8810 web` (캐시 무효화 서버) |
| 정의 위치 | `.claude/launch.json` |
| 원문 자료 | `source/body info.svg`(신체 부위별 검진 항목·가격표, A4 300dpi), `source/background.eps`(배경 디자인 원본, Illustrator EPS), `source/첨부2. '25년 건진 캠페인 홍보물.pdf`(2025년도 같은 캠페인 실제 홍보물 — 구성·문구·2025년 가격의 참고 원본), `source/card design.svg`(Affinity Designer 산출물, 사용자가 `M:\home\2026\0903 종합건진 캠페인\`에서 전달 — 카테고리 박스의 새 디자인 기준: 흰 카드+하늘색 테두리+그림자+제목 아래 구분선) — 모두 읽기 전용 원자료 |
| 변경 이력 | `CHANGELOG.md` |

### 사이트 구조

**2026-09-11부터: 아래 "1차 초안" 서술은 전부 지나간 역사다.** 사용자가 공식 포스터(`source/캠페인 포스터-outlined.pdf`/`.svg`, 4페이지: 표지/캠페인 프로그램/추가검사 항목/VIP 건진 패키지)를 전달했고, "캠페인 페이지를 첨부한 PDF와 페이지 순서를 기준으로 반영"을 요청해 `web/index.html`을 그 4페이지를 그대로 보여주는 구조로 전면 재구성했다. 지금의 정확한 파일 목록·작업 규칙은 [CLAUDE.md](CLAUDE.md)의 "핵심 파일"/"작업 기준"에 있으니 그쪽을 먼저 본다. 아래 히스토리는 "왜 이렇게 됐는지"를 추적할 때만 필요하며, 지금 화면이 어떻게 구성되는지는 설명하지 않는다(예: `body-info.svg`, `program.js`, 카테고리 그룹 `id="category-{slug}"` 등은 전부 이제 안 쓰이는 예전 방식이다).

- `web/index.html` — 랜딩 1페이지. 2025년도 홍보물 PDF의 구성(제목·기간·EVENT 2종·종합건진 프로그램 표·상담 연락처·추가검사 항목)을 그대로 옮긴 1차 초안이다.
  - "추가검사 항목" 절은 `source/body info.svg`(텍스트가 전부 패스로 고정된 이미지) 원본의 **신체 일러스트·박스 배치·구도는 그대로 두고 텍스트 부분만** 실제 폰트 `<text>`로 교체한 사본(`web/assets/source/body-info.svg`)을 만들어 반영했다. 값은 2025년과 동일하게 확정된 내용이다. 2026-09-03부터는 이 사본을 인라인이 아니라 `<object type="image/svg+xml" data="assets/source/body-info.svg">`로 외부 참조한다(§"원본 자료 취급" 참고).
  - 섹션 제목("추가검사항목"), 영문 부제("Extra examinations"), 하단 안내문("- 추가검사 사전예약 필수, 당일 검사 추가는 어렵습니다. -"), 장식 점 6개도 전부 이 SVG 안(`<g id="pageTitleGroup">`, `<g id="decorDots">`)에 있다. 사용자가 참고 파일에 직접 그려 넣은 디자인을 좌표·글자 크기(최종 렌더 기준 제목 175px·부제 70px·안내문 60px)만 옮겨 반영했고, 원본은 글자 하나하나를 개별 tspan으로 배치했지만 이쪽은 `letter-spacing`을 준 단일 `<text>`로 단순화했다. 이 제목이 생기면서 기존에 `web/index.html`에 따로 두던 "02" 번호 배지(`.section-badge`)와 개발용 안내 문단(`.exam-source-note`)은 제거했다 — 섹션 제목·안내문은 이제 SVG 쪽에서만 관리한다.
  - 카테고리 박스 자체의 디자인은 이후 `source/card design.svg` 기준으로 다시 바꿨다. 흰 배경 카드(모서리 radius 46, 하늘색 `rgb(152,233,255)` 테두리 5px, 부드러운 그림자)에 제목(`rgb(0,118,190)`)과 그 아래 파란 구분선(`rgb(0,118,190)`, 5px)만 있는 형태로, 기존의 색칠된 헤더 배지(파랑/연파랑 두 톤)와 장식 tick 마크는 없앴다. 항목·가격 텍스트의 폰트 크기·색은 바꾸지 않았다.
  - "02 추가검사 항목" 섹션(`.exam-section`) 배경은 밝은 하늘색(`#e1f3ff`)이고, 그 위에 캔버스 파티클 배경 효과(`web/scripts/particles.js`, 밝은 회색 입자+연결선, [참고](https://techhub.iodigital.com/articles/particle-background-effect-with-canvas))가 깔려 있다. 흰 카드 배경 위로는 파티클이 보이지 않고(카드가 불투명), 카드 사이 여백과 신체 일러스트 주변에만 은은하게 비친다. 관련 CSS(`.exam-section`, `.exam-svg-wrap` z-index, `.exam-particles` 캔버스 스타일)는 2026-09-03부터 `app.css`가 아니라 별도 파일 `web/styles/exam-particles.css`에 있고, `web/index.html`이 `<link rel="stylesheet">`로 불러온다.
  - 신체 일러스트 위에는 6개 신체 부위(뇌·심혈관·폐·갑상선·소화기·비뇨기)를 표시하는 반투명 빨강(`rgba(230,0,0,0.8)` 점 + `rgba(230,0,0,0.45)` 동심원) 강조 마커가 있고, 각 마커에서 해당 카테고리 박스 가장자리로 **수평 구간 → 대각선 구간**으로 꺾이는 연결선(`rgba(21,84,155,0.5)`, 중간 꺾임점은 마커-박스 x거리의 중점)이 이어진다. 마커·연결선은 `web/assets/source/body-info.svg`의 `<g id="bodyPointMarkers">`(2026-09-03 재구조화 이전 이름은 `bodyPoints`)에 있다. 동심원 애니메이션 CSS(`@keyframes body-point-ripple`, 3초 주기·0/1/2초 딜레이 3겹)는 2026-09-03부터 SVG 내부 `<style>`이 아니라 별도 파일 `web/styles/body-points.css`에 있고, `body-info.svg` 맨 위의 `<?xml-stylesheet href="../../styles/body-points.css" type="text/css"?>` 처리 명령으로 불러온다(SVG가 `<object>`로 로드돼 자기 문서 컨텍스트를 가지므로 가능함 — SVG 안의 `<style>`에는 `@font-face`만 남음). 좌표는 사용자가 전달한 `C:\Users\Mini\SynologyDrive\2026\0903 종합건진 캠페인\body-info.svg`에 미리 찍힌 6개 강조점 위치를 그대로 옮긴 것이며, 이후 사용자가 같은 참고 파일을 직접 수정해 4개 지점(심혈관·폐·소화기·비뇨기)의 위치/연결점을 다시 조정한 값을 한 번 더 반영했다(뇌·갑상선은 최초 값 유지). 애니메이션 자체는 건드리지 않고 좌표만 옮기는 방식으로 처리했다.
  - 심혈관계·내분비계 두 박스는 자리를 맞바꿨다(오른쪽 열: 내분비계가 위 `y302.6`, 심혈관계가 아래 `y912.6`). 실제 심장 위치(가슴 중앙)와 가까운 자리로 심혈관계를 옮겨 강조선 길이를 줄이기 위함이다. 두 박스 사이 간격(276.7)은 그대로 유지했고, 항목 수가 다른 두 박스를 맞바꾼 것이라 순서를 뒤집어도 전체 세로 폭이 정확히 같다(높이 합 + 간격이 교환 전후로 동일).
  - 근골격계(원래 왼쪽 열)·부인과(원래 오른쪽 열)도 서로 자리를 맞바꿨다. 이번엔 열이 서로 달라 각자의 (x,y) 좌표를 통째로 맞바꾸는 방식으로 처리했다(근골격계 → 옛 부인과 자리 `x1461.2,y2643.1`, 부인과 → 옛 근골격계 자리 `x127.7,y2079.8`). 두 박스 다 강조 마커 연결선의 대상이 아니라 마커 쪽은 손대지 않았다.
  - 이후 왼쪽 열(뇌·폐·비뇨기계·부인과·PET-CT)과 오른쪽 열(내분비계·심혈관계·소화기계·근골격계)을 각각 **상단은 뇌/내분비계(`y302.6`), 하단은 PET-CT/근골격계(`y3114.5`)로 정렬**하고, 그 사이 박스는 높이 합을 뺀 나머지 공간을 간격 개수만큼 균등 분배했다(왼쪽 4칸 간격 195.8, 오른쪽 3칸 간격 275.97). 현재 최종 배치:
    - 오른쪽: 내분비계 `y302.6` → 심혈관계 `y911.9` → 소화기계 `y1742.4` → 근골격계 `y2643.1`
  - 카드·구분선·제목·항목 좌표를 전부 옮기는 스크립트는 각 치환이 "정확히 1번만 매치"되도록 강제해서 조용히 실패하는 일이 없게 짰다(이전 스왑에서 있었을 수 있는 그런 실패를 이번에 다시 계산하며 함께 바로잡음). 박스를 또 옮길 때는 이 좌표를 새 "현재값"으로 본다.
  - 이후 마지막 2줄 항목 3개(뇌MRI&MRA+경동맥 MRA, 알츠하이머 위험도 혈액검사, 저선량 폐 MDCT)를 전부 1줄로 합치면서 뇌(머리)·폐 박스 높이가 줄어(각각 6/1개의 1줄 항목만 남음), 왼쪽 열 전체를 다시 상/하단 정렬 + 균등 간격으로 재계산했다. 표준 값: 헤더→첫 줄 간격 106, 줄간격 81, 카드 위 여백 58·아래 여백 64(예: 근골격계 4줄 항목 기준 높이 471.4 ≈ 58+106+3×81+64). 최종 왼쪽 열 배치: 뇌(머리) `y302.6`(h633.0, 6줄) → 폐 `y1163.6`(h228.0, 1줄) → 비뇨기계 `y1619.6`(h330.2) → 부인과 `y2177.7`(h380.6) → PET-CT `y2786.3`(h328.2), 간격 227.975로 균등. 신체 강조 마커 중 뇌·폐·비뇨기계 3개는 박스가 움직여 연결선 끝점(박스 세로 중앙)도 함께 재계산했다: 뇌→`992.1,619.1`, 폐→`992.1,1277.6`, 비뇨기→`992.1,1784.7`(심혈관·갑상선·소화기는 오른쪽 열이라 변경 없음).
  - **2026-09-03(같은 날 늦게)**: 사용자의 최신 참고 파일(`C:\Users\Mini\SynologyDrive\2026\0903 종합건진 캠페인\body-info.svg`)과 대조한 결과, 위 바로 앞 항목들에서 다룬 좌표(y302.6/y1163.6/y911.9 등)가 실제로는 전부 185.5만큼 낮게 그려지고 있었음을 발견함. 원인: 제목을 추가하며 신체 일러스트와 강조 마커 그룹만 185.5 내렸을 뿐(§ 앞부분), 카드·구분선·헤더·항목 텍스트는 그 이동을 반영하지 못한 채 예전 좌표에 남아 있었다 — 그 결과 카드 상단이 부제("Extra examinations") 아래쪽과 겹치고, 마지막 카드와 하단 안내문 사이에 불필요한 큰 여백이 생겨 있었다. 참고 파일의 카드/구분선/커넥터 라인 좌표(이미 +185.5가 반영된 최종값)와 헤더·항목 텍스트 좌표를 그대로 대조해, 우리 쪽 카드·구분선·헤더·항목 전체에 +185.5를 적용해 맞췄다(신체 일러스트 transform도 x값 `-190.224`→`-178.07`로 함께 보정). 이 작업과 동시에 `Artboard11` 바로 아래 평평하게 나열돼 있던 ~100개 요소(카드 9·구분선 9·헤더 10·항목 60·장식 점 6, 장식 점은 이미 최종 좌표였음)를 9개의 `<g id="category-{slug}" aria-label="{카테고리명}">`로 묶어 카테고리 단위로 유지보수할 수 있게 재구조화했고, 원본 export 잔재였던 빈 `<g>` 셸(글자를 패스→텍스트로 바꾸며 남은 빈 껍데기, 9개 카테고리에 총 600개 이상의 빈 하위 그룹) 도 함께 제거함. 신체 부위 마커 6개는 처음엔 각 카테고리 그룹 안에 같이 넣었으나, 사용자가 "항상 카드보다 위 레이어에 그려져야 한다"고 지적해 `bodyPointMarkers`라는 별도 그룹으로 다시 빼고 문서 맨 끝(모든 카테고리보다 나중에 그려지는 자리)에 배치했다 — 각 마커에는 `data-category="category-brain"` 같은 속성을 남겨 소속 카테고리를 추적할 수 있게 함. 현재 정확한 좌표값은 [CLAUDE.md](CLAUDE.md) "작업 기준"에 있다.
  - **2026-09-03**: 사용자 요청("각 페이지는 각각의 페이지로 A4 사이즈 PDF로 변환")에 따라 인쇄/PDF 저장 시 페이지 분리 기능을 추가함. 콘텐츠를 두 `<div class="pdf-page">`/`<section ... pdf-page">`로 나누고(1페이지: 헤드라인+EVENT+01 종합건진 프로그램+연락처, 2페이지: 02 추가검사 항목 SVG), `app.css`의 `@media print`에 `.pdf-page{break-after:page}`(마지막 페이지 예외)와 `.card,.event-card,.program-card{break-inside:avoid}`(카드가 페이지 경계에서 안 잘리게)를 추가함. 기존 상단바의 "🖨 인쇄 / PDF" 버튼(`window.print()`)이 그대로 이 규칙을 적용받는다. 02 페이지는 `body-info.svg`의 viewBox 비율(2481:3508)이 A4 비율과 정확히 같아 별도 스케일 조정 없이 한 장에 맞는다.
  - **2026-09-03(이어서)**: "01 종합건진 프로그램" 표도 "02 추가검사 항목"과 같은 엑셀 기반 구조로 바꿈("엑셀데이터베이스 기반 자료 업데이트 구조로 변경" 요청). `data/종합건진프로그램.xlsx`(프로그램명/구분/가격(만원)/정상가(만원)/비고) → `scripts/sync_program_items.py` → `web/data/program.js`(`window.PROGRAM_ITEMS`) → `web/scripts/program.js`가 `#programGrid`를 직접 렌더링하는 흐름. SVG가 아니라 순수 HTML이라 좌표 동기화가 필요 없어 02보다 훨씬 단순한 파이프라인이다. 엑셀 값을 임시로 바꾸고 재실행→원복하는 왕복 테스트로 전체 흐름을 검증함.
  - **2026-09-03(이어서)**: 사용자 요청("전체 페이지 svg 파일로 출력", 범위/방식은 AskUserQuestion으로 확인: 01+02 모두, 실제 벡터 재구성)에 따라 `web/assets/source/program-page.svg`를 신설함 — "01 종합건진 프로그램" 페이지(헤드라인·EVENT·프로그램표·연락처)를 `body-info.svg`와 같은 2481×3508 A4 캔버스 규격의 벡터 SVG로 재현한 것. 측정은 body-info.svg 제작 때와 같은 기법(브라우저를 인쇄 폭 733px로 맞추고 `getBoundingClientRect`+`getComputedStyle`로 절대좌표 측정 후 스케일 변환)을 썼다. `web/index.html`과는 연결돼 있지 않은 **스냅샷 산출물**이라, `program.js`/엑셀 데이터가 바뀌어도 자동 갱신되지 않는다 — 최신본이 다시 필요하면 같은 측정을 재실행해야 한다.
  - 그 외 캠페인 기간, EVENT 할인율·조건, 종합건진 프로그램 가격, 상담 전화번호는 **2025년 참고값**이며 화면에 `2025 참고` / `2026 확인 필요` 라벨을 붙여뒀다. 2026년 확정 값을 받으면 이 라벨과 함께 교체한다.
  - 배경 디자인(`source/background.eps`)은 변환 도구가 없어 아직 반영하지 못했다.
- 홍보물이 여러 종류(랜딩 + 카드뉴스 + 포스터 등)로 늘어나면 §2-2 매니페스트(`web/data/site.js`) + 공용 엔진(`web/scripts/common.js`) 구조를 적용한다. 지금은 페이지가 1개뿐이라 적용하지 않았다.

### 원본 자료 취급

**2026-09-11부터: 원본이 `source/캠페인 포스터-outlined.svg`(공식 포스터, 4개 아트보드)로 바뀌었다.** 각 아트보드를 뽑아내는 절차(viewBox 계산, `url(#id)` 참조 추적, 공유 폰트 스타일시트 연결)는 [CLAUDE.md](CLAUDE.md)의 "포스터가 갱신되면..." 항목에 정리해 뒀다. 아래의 `body info.svg`/`body-info.svg` 관련 서술은 이제 안 쓰이는 예전 파이프라인이다(파일은 참고용으로 남겨뒀다 — CLAUDE.md "이제 쓰이지 않는 예전 파일" 참고).

- 수정 원본은 `source/`이며 **수정하지 않는다**(읽기 전용 원자료).
- 웹에서 바로 쓸 수 있는 사본은 `web/assets/source/`에 둔다. 복사 방향은 `source/ → web/assets/source/` 한 방향뿐이다. 원본이 교체되면 사본도 함께 갱신한다.
- `source/body info.svg`(텍스트가 패스로 고정된 원본) → `web/assets/source/body-info.svg`(텍스트를 `<text>`로 교체한 사본, 파일명 공백을 하이픈으로 변경)로 가공해 복사했다. 가공 방법: 신체 일러스트·`<rect>`(헤더 배지·장식 tick)는 그대로 두고, 각 카테고리 그룹 안의 글자 패스(`<path>`)만 제거한 뒤 브라우저의 `getBoundingClientRect`+`getScreenCTM` 역행렬로 원래 글자 줄의 정확한 좌표를 구해 같은 자리에 `<text>`(폰트: 프로젝트 표준 Noto Sans KR, 색은 원본과 동일한 `rgb(59,125,163)`/흰색)를 배치했다.
- `web/index.html`에는 이 사본을 **`<object type="image/svg+xml" data="assets/source/body-info.svg" class="exam-object">`로 외부 참조**한다(2026-09-03부터, "css와 디자인 자료를 분리 모듈화" 요청으로 인라인 삽입에서 전환). `<img src>`는 이미지 컨텍스트라 SVG 내부의 `@font-face`(외부 폰트)를 못 불러오므로 쓸 수 없고, `<object>`는 자기 문서 컨텍스트를 가져 `@font-face`가 정상 로드된다. 이 때문에 `body-info.svg` 파일 자신이 맨 위 `<style>`에 `@font-face`(경로 `../../fonts/NotoSansKR-Regular.ttf`, `-Bold.ttf`, SVG 파일 위치 기준 상대경로)를 자체적으로 갖고 있다 — 페이지의 전역 폰트 상속에 기대지 않는다.
- 이 가공 절차는 `source/body info.svg` 특유의 좌표(그룹별 변환행렬, 줄 위치)에 맞춰진 1회성 작업이다. 원본 그래픽이 바뀌면 이 절차를 처음부터 다시 해야 하며, 단순 재실행 스크립트로 재사용할 수 없다.
- **`web/assets/source/body-info.svg`를 고치면 그것으로 끝이다.** `web/index.html`이 이 파일을 `<object>`로 외부 참조만 하므로 "인라인 사본에도 반영" 같은 별도 동기화 단계가 없다(예전에는 인라인 `<svg>...</svg>` 블록을 통째로 교체해야 했고, 일부 구간만 문자열 치환하면 두 사본이 어긋나 박스가 엉뚱한 자리에 겹쳐 그려지는 사고가 났었다 — 2026-09-03 실제 발생·수정 — 이제는 파일이 하나뿐이라 이 사고 자체가 구조적으로 불가능하다). 저장 후 브라우저 새로고침으로 바로 확인한다.
- **박스를 옮길 때 "좌표값만" 문자열 치환하는 방식은 SVG의 그리기 순서(파일에 나온 순서대로 위에 덮어 그림) 문제를 놓칠 수 있다.** 카드 rect와 구분선 path는 원래 [카드, 구분선] 순서로 나란히 있어 구분선이 카드 위에 그려지는데, 여러 번 스왑을 거치며 "값"만 옮기고 "요소 위치"는 그대로 두면 어느 구분선이 우연히 자기보다 뒤에 그려지는 다른 카드와 좌표가 겹쳐 그 카드에 가려질 수 있다(2026-09-03: 부인과 구분선이 이 문제로 안 보이는 사고가 실제 발생, ElementTree로 구분선 요소를 자기 카드 바로 뒤로 옮겨 수정). 박스 위치를 옮긴 뒤에는 좌표값 검증뿐 아니라, 각 구분선이 자기보다 뒤에 그려지는 카드와 겹치지 않는지(= 가려지지 않는지)도 함께 확인한다.
- 항목명·가격을 사람이 편집할 수 있도록 `data/추가검사항목.xlsx`(구분/항목명/가격(원)/비고, 9개 계열 30행)를 별도로 관리한다.
- `web/assets/source/body-info.svg`의 각 `<text>`에는 `id="exam-{엑셀행번호}-label"`(2줄인 항목만 `-label2` 추가) / `id="exam-{엑셀행번호}-price"`가 붙어 있다(엑셀 2행=1번 ... 31행=30번). 이 id는 원본 디자인의 줄 위치에 맞춰 **한 번만 수동으로** 붙인 것이라, 항목을 추가·삭제하면 SVG 쪽 좌표 작업을 다시 해야 한다(단순 재실행으로 늘어나지 않음).
- 엑셀 → 화면 반영은 `scripts/sync_exam_items.py`로 한다. 엑셀 값을 고친 뒤 `python scripts/sync_exam_items.py`를 실행하면 `body-info.svg` 하나의 해당 `<text>`를 갱신한다(`web/index.html`은 이 파일을 `<object>`로 참조만 하므로 별도 반영 단계 없음). 반대 방향(SVG → 엑셀)은 지원하지 않으므로, 항목·가격은 **항상 엑셀에서 먼저 고친다.**
- 원본 디자인이 2줄로 배치해 둔 항목(뇌MRI&MRA+경동맥 MRA, 알츠하이머 위험도 혈액검사, 저선량 폐 MDCT)은 이름이 바뀌면 스크립트가 자동으로 줄바꿈 위치를 다시 잡지 못하고 1줄로 합쳐 넣은 뒤 경고를 출력한다. 이 세 항목의 이름을 바꿀 때는 실행 후 화면을 확인하고 필요하면 SVG의 `-label`/`-label2` 줄바꿈을 손으로 나눠준다.
- `source/background.eps`는 **이 작업 환경(브라우저 기반 에이전트)에서 렌더링·변환할 수 없다.** Illustrator EPS 포맷은 Ghostscript/ImageMagick/Inkscape 중 하나가 있어야 래스터화(PNG/JPG 변환)가 가능한데, 이 머신에는 셋 다 설치돼 있지 않다(확인일 2026-09-03). PNG/JPG로 내보낸 사본을 받거나, 별도 변환 도구를 설치한 뒤에 `web/assets/source/`에 반영한다. 그 전까지 `web/index.html`의 배경은 표준 테마 색상으로 대체한다.

### 콘텐츠 취급 규칙

**2026-09-11부터**: 캠페인 기간·할인율·프로그램 가격·연락처는 `source/캠페인 포스터-outlined.svg`(공식 확정 포스터)의 값이다. 더 이상 "2025년 참고값"이 아니므로 `2025 참고`/`2026 확인 필요` 라벨을 새로 붙이지 않는다. 검진 항목명·가격 등 실제 수치는 포스터 SVG에 이미 포함된 값만 쓰고, 포스터에 없는 항목·가격을 추측해 추가하지 않는다. 포스터 자체가 바뀌면(사용자가 새 버전을 전달하면) 그 값으로 다시 추출해 교체한다.
- 라벨이 붙지 않은 원문 문구를 그대로 베낄 때(예: 검사 설명)도 OCR/추출 품질을 확신할 수 없으면 "원문 대조 필요"를 함께 남긴다.

---

# 공통 기준 원문

아래는 `바이브코딩 기준/AGENTS.md`의 공통 기준이다. 위 프로젝트 적용값과 다르면 **위쪽 적용값이 우선**한다.

---

## 1. 기본 원칙: 카파시룰 (Karpathy 바이브코딩 원칙)

모든 작업은 아래 원칙을 **기본값**으로 따른다. 이 원칙은 설치된 플러그인 `andrej-karpathy-skills`의 `karpathy-guidelines` 스킬(Andrej Karpathy가 정리한 LLM 코딩 실수 방지 가이드라인, MIT)을 기준으로 한다. Claude Code에서 작업할 때는 코드 작성·리뷰·리팩토링 시 이 스킬을 활성화해 사용한다.

### 1-0. 스킬 우선 활용

작업을 시작할 때는 설치된 스킬을 먼저 확인하고, 해당하는 스킬이 있으면 적극적으로 사용한다. 특히 아래 두 계열은 기본 작업 프로세스에 포함한다.

- **호출 전에 설치 여부를 확인한다.** 스킬 이름을 기억에 의존해 호출하지 말고, 현재 세션에서 실제로 사용 가능한 스킬 목록을 먼저 확인한 뒤 그 안에서 고른다. 없는 스킬을 호출하면 작업이 실패하거나 조용히 무시된다.
- **Karpathy 스킬 — 설치됨**: 코드 작성·리뷰·디버깅·리팩토링·설계 판단에는 `andrej-karpathy-skills:karpathy-guidelines`를 우선 적용한다. 가정, 단순성, 외과수술식 변경, 검증 기준을 작업 전에 명확히 한다.
- **Superpowers 스킬 — 이 머신에는 미설치**: `superpowers:*`(`using-superpowers`, `brainstorming`, `systematic-debugging`, `test-driven-development`, `writing-plans`, `verification-before-completion`, `requesting-code-review`)는 2026-08-27 기준 설치돼 있지 않다. 설치된 환경에서는 작업 유형이 분명할 때 우선 활용하고, **설치돼 있지 않으면 같은 이름을 호출하지 말고 아래 대체 절차를 그대로 수행한다.**
  - 계획 수립 → 착수 전 `[단계] → 검증: [확인 방법]` 형태의 짧은 계획을 먼저 제시한다.
  - 디버깅 → 재현 → 원인 가설 → 최소 수정 → 재현 케이스 통과 순서로 진행한다.
  - 완료 전 검증 → 5장의 검증 기준을 생략 없이 실행한다.
  - 코드 리뷰 → 변경된 모든 줄이 요청으로 설명되는지(1-3) 스스로 대조한다.
- **스킬 선택 순서**: 사용 가능한 프로세스 스킬을 먼저 고르고(없으면 위 대체 절차), 그 위에 구현·검증 원칙(Karpathy)을 적용한다.

> **설치 현황 (2026-08-27 확인)** — 플러그인: `andrej-karpathy-skills@karpathy-skills` 1개. 마켓플레이스: `claude-plugins-official`, `karpathy-skills` 2개. `~/.claude/skills/`에는 의학 연구·논문 계열 사용자 스킬(`search-lit`, `write-paper`, `analyze-stats` 등)이 있으나 웹 자료 작업과 무관하므로 이 기준의 작업에서는 호출하지 않는다. 설치 목록이 바뀌면 이 항목을 함께 갱신한다.
- **적용 예외**: 사용자가 명시적으로 스킬 사용을 원하지 않거나, 작업이 단순한 파일 조회·명령 결과 확인처럼 스킬 적용이 실질적 이득이 없는 경우에는 짧게 생략할 수 있다. 다만 코드 변경, 문서 기준 변경, 빌드·배포·검증 작업에서는 기본적으로 생략하지 않는다.
- **보고 방식**: 스킬을 사용한 경우 어떤 스킬을 왜 사용했는지 한 줄로 알리고, 결과 보고에는 실제 검증 내용과 남은 리스크를 함께 적는다.

### 1-1. 코딩 전에 생각한다 (Think Before Coding)

**가정하지 말고, 혼란을 숨기지 말고, 트레이드오프를 드러낸다.**

- 구현 전에 자신의 가정을 명시적으로 밝힌다. 불확실하면 질문한다.
- 해석이 여러 가지면 조용히 하나를 고르지 말고 선택지를 제시한다.
- 더 단순한 방법이 있으면 말한다. 필요하면 사용자 요청에도 반론을 제기한다.
- 불명확한 것이 있으면 멈추고, 무엇이 혼란스러운지 짚고, 질문한다.

### 1-2. 단순함 우선 (Simplicity First)

**문제를 푸는 최소한의 코드만. 추측성 코드는 금지.**

- 요청받지 않은 기능을 추가하지 않는다.
- 한 번만 쓰는 코드에 추상화를 만들지 않는다.
- 요청받지 않은 "유연성"·"설정 가능성"을 넣지 않는다.
- 일어날 수 없는 시나리오에 대한 에러 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 가능하면 다시 쓴다.
- 자문한다: "시니어 엔지니어가 보면 과하게 복잡하다고 할까?" 그렇다면 단순화한다.
- 이 기준에서는: 프레임워크·빌드 도구 없이 정적 HTML/CSS/JS로 시작하고, 필요가 증명된 뒤에만 도구를 추가한다.

### 1-3. 외과수술식 변경 (Surgical Changes)

**꼭 필요한 부분만 건드리고, 내가 만든 흔적만 치운다.**

- 변경 대상 주변의 코드·주석·포맷을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩토링하지 않는다.
- 내 취향과 달라도 기존 스타일을 따른다.
- 무관한 죽은 코드를 발견하면 삭제하지 말고 언급만 한다.
- 내 변경으로 안 쓰이게 된 import/변수/함수는 제거하되, 원래 있던 죽은 코드는 요청 없이는 제거하지 않는다.
- 판정 기준: 변경된 모든 줄이 사용자의 요청으로 직접 설명돼야 한다.

### 1-4. 목표 중심 실행 (Goal-Driven Execution)

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

- 작업을 검증 가능한 목표로 바꾼다. 예: "버그 수정" → "버그를 재현하는 테스트를 만들고 통과시킨다".
- 여러 단계 작업은 단계마다 `[단계] → 검증: [확인 방법]` 형태의 짧은 계획을 먼저 밝힌다.
- 이 기준에서는: 웹 자료 변경의 성공 기준은 **미리보기 화면**이다. 변경 직후 미리보기(스냅샷·콘솔 에러)로 검증한 뒤 다음 단계로 넘어가고, 검증 없이 변경을 쌓지 않는다.
- 콘솔 에러·서버 로그는 에이전트가 직접 읽고 원인을 찾아 고친 뒤 결과만 보고한다.

> 트레이드오프: 이 원칙들은 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단에 따라 완화한다.

## 2. 작업 프로세스

모든 작업은 아래 4단계 사이클로 진행한다.

```
① 자료 확인 → ② 웹 자료 생성 → ③ 실시간 미리보기 → ④ 요청 시 갱신 (③으로 복귀)
```

### ① 자료 확인
- 작업 시작 전 프로젝트 폴더의 **현재 생성 자료**(기존 HTML/CSS/JS, 참고자료, 문서)를 먼저 파악한다.
- 기존 자료가 있으면 그 구조·스타일·네이밍을 따르고, 처음부터 새로 만들지 않는다.

### ② 웹 자료 생성
- 프론트엔드 자료(HTML, CSS, JS, 폰트, 정적 파일)는 항상 **원본 폴더 하나**(`web/` 권장)에만 생성·수정한다.
- 빌드 사본, 임시 미리보기 복사본(`/tmp/...`)은 원본이 아니다. 항상 원본을 수정한 뒤 사본을 동기화한다.
- 새 프로젝트를 시작할 때는 원본 폴더와 미리보기 서버 정보를 먼저 정한다. 정하지 않은 상태에서 여러 위치에 파일을 만들지 않는다.

### ②-0. 원본 폴더와 실행기 폴더 분리
- 웹 자료의 수정 원본은 항상 `web/` 같은 원본 폴더에 둔다.
- `exe/`, `app/`, `server/`, `dist/`, `webroot/`처럼 실행·배포·임베드에 쓰는 폴더는 관제 또는 산출물 폴더로 취급한다.
- 실행기 폴더에서는 원본 자료를 직접 수정하지 않는다. 필요한 경우 `sync`, `generate`, `build`, `preview` 명령으로 원본을 복사·임베드·검증한다.
- 원본에서 실행기 산출물로 복사되는 구조라면 복사 방향을 문서화한다. 예: `web/ → exe/webroot/ → 실행 파일 embed`.
- 실행기 폴더에 `dev.sh`, `Makefile`, `taskfile` 같은 관제 스크립트를 둘 수 있다. 이 스크립트는 동기화, 미리보기, 검증, 빌드를 한 곳에서 실행하기 위한 진입점이어야 한다.
- 관제 스크립트는 프로젝트의 등록 포트 1개만 사용해야 하며, 포트가 사용 중이면 다음 빈 포트를 자동 선택하지 말고 기존 프로세스를 재사용·정리하거나 중단한다.

### ②-0-1. 개발 모드와 배포 모드 분리
- 개발 중에는 `web/` 원본을 직접 미리보기한다. 프론트엔드 수정마다 실행기 산출물 생성, 임베드, 컴파일을 반복하지 않는다.
- 서버/API 확인이 필요한 경우 실행기를 **개발 모드**로 실행해 `webroot/` 산출물이 아니라 `../web` 같은 원본 폴더를 직접 서빙한다.
- `webroot/`, embed 파일, 배포용 실행 파일은 **배포 모드** 산출물이다. 빌드 직전 또는 전달 직전에만 동기화·생성한다.
- 백엔드 코드가 바뀌지 않은 일반 UI 수정은 `web/` 미리보기와 JS 문법 검사로 끝낸다. 실행기 재빌드는 하지 않는다.
- 로그인, 설정 저장, 파일 생성, 다운로드, DB/API 호출처럼 백엔드를 거치는 기능만 개발 모드 실행기에서 추가 확인한다.
- 권장 관제 명령은 아래처럼 역할을 분리한다.

```sh
sh dev.sh web          # web/ 원본 정적 미리보기
sh dev.sh app          # 실행기 개발 모드: 원본 web/ 직접 서빙 + API 확인
sh dev.sh sync         # web/ → webroot/ 동기화
sh dev.sh build x.y.z  # 동기화 + 검증 + 배포 산출물 생성
```

### ②-0-2. web → webroot → embed 동기화 시 누락 방지 (Go 프로그램)

`web/`을 Go 실행 파일에 `//go:embed`로 내장하는 프로젝트는 **검진팀 대시보드 방식(전체 복사형)을 표준으로 한다.** 파일명을 하나씩 지정하는 allowlist 방식은 새 파일·폴더를 목록에 추가하지 않으면 에러 없이 조용히 빠지므로 쓰지 않는다.

```go
//go:generate sh -c "rm -rf webroot && cp -R ../web webroot && find webroot -name .DS_Store -delete"
//go:embed all:webroot
```

- `web/` 전체를 그대로 복사하므로 새 파일·폴더를 추가해도 누락되지 않는다. `all:` 접두사는 필수(없으면 점·밑줄로 시작하는 파일이 빠진다).
- `go build`를 직접 호출하지 않고 항상 `build.sh`(`go generate ./...` 선행 포함)를 거친다.
- 빌드 후 5-1의 "실행기 검증 방법"으로 실제 실행 파일에 반영됐는지 한 번 확인한다.

**이미 선별 복사 스크립트를 쓰는 프로젝트**(`종합검진센터 스케쥴관리`의 `application_beta/exe/sync-webroot.py` — 최상위 `*.html` 자동 발견 + `styles`·`js`·`data`·`fonts` 폴더 단위 복사, `건강검진 제안서 자동생성기`의 `exe/sync-webroot.py` — `web/ → exe/webroot/` 및 `web/fonts/ → exe/resources/`)는 표준으로 갈아엎기보다 **동기화 직후 누락 검증을 반드시 실행하는 쪽**으로 운용한다. 선별 복사는 새 폴더가 생겼을 때 에러 없이 조용히 빠지므로, 검증 없는 선별 복사는 금지다.

- 새 HTML·JS·CSS·data·fonts 파일을 추가하거나 파일을 분리한 경우, 동기화 후 `web/`과 산출물 폴더의 파일 목록을 실제로 대조한다.
- 누락 파일이 하나라도 있으면 완료·빌드·배포 보고를 하지 않는다.
- `setting/`의 실데이터·관리자 해시·백업 파일, 테스트 폴더, OS 보조 파일(`.DS_Store`, `._*`)은 실행 파일에 내장하지 않는다.
- 선별 복사 스크립트를 새로 만드는 경우에는 위 전체 복사형을 먼저 검토하고, 그래도 선별이 필요하면 이유를 스크립트 주석에 남긴다.

### ②-1. 자료 모듈화
- 화면에 들어가는 원본 데이터·문구·설정은 가능한 한 HTML 구조와 분리한다.
- 반복되는 항목(일정, 표 행, 카드, FAQ, 탭 콘텐츠 등)은 `data/`, `content/`, `modules/` 중 프로젝트의 기존 폴더 규칙에 맞춰 별도 파일로 관리한다.
- 기존 프로젝트에 데이터 모듈 규칙이 있으면 그 규칙을 따른다. 없으면 정적 자료는 `web/data/`, 화면 조립 JS는 `web/scripts/`에 두는 것을 기본값으로 한다.
- 같은 내용을 HTML, JS, JSON에 중복으로 적지 않는다. 한 곳의 원본을 읽어 화면을 렌더링한다.
- 모듈을 나눌 때는 실제로 재사용되거나 갱신 단위가 분리되는 경우에만 나눈다. 한 번만 쓰는 작은 문구까지 과하게 분리하지 않는다.
- 첨부 자료를 반영할 때는 원문 의미를 보존하고, 요약·재배열·표준화한 내용은 데이터 파일에서 구조가 드러나게 둔다.

### ②-2. 페이지가 여러 개인 정적 사이트: 매니페스트와 공용 엔진

페이지가 2개를 넘어가면 페이지마다 HTML을 복제하지 않는다. `검진팀 상용 AI 서비스`에서 검증된 아래 구조를 기본값으로 한다.

- **사이트 매니페스트**: `web/data/site.js` 한 곳에 사이트명·갱신일·페이지 목록을 두고(`window.SITE`, `window.DECKS`), 랜딩 카드와 페이지 간 교차 링크를 이 목록에서 자동 생성한다. 사이트명·갱신일·페이지 수를 각 HTML에 하드코딩하지 않는다.
- **공용 렌더링 엔진**: 모든 페이지가 공유하는 로직(아이콘, 카드, 목차, 페이지 번호, 내비게이션)은 `web/scripts/common.js` 하나에 두고, 페이지별 파일은 데이터와 전용 로직만 갖는다.
- **페이지 추가 순서**: ① `web/data/<영역>.js`에 기존 스키마 그대로 데이터 추가 → ② `site.js`의 페이지 목록에 항목 하나 추가 → ③ 기존 페이지를 본뜬 HTML을 `web/subpage/`에 추가 → ④ 전용 스크립트는 공용 엔진 함수만 호출 → ⑤ 공용 엔진(`common.js`)은 건드리지 않는 것을 목표로 한다.
- 이 순서를 벗어나 HTML에 값을 직접 박아 넣지 않는다.

### ③ 실시간 미리보기
- 아래 **3. 서버 규칙**에 따라 **등록된 서버 1개**로만 미리보기한다.
- 정적 파일이므로 서버 재시작 없이 브라우저 새로고침으로 변경을 확인한다.
- UI 변경 후에는 스냅샷/스크린샷과 콘솔 에러를 확인한 뒤 완료를 보고한다.

### ④ 요청 시 갱신
- 사용자의 수정 요청을 받으면 원본 폴더의 자료를 갱신하고, 실행 중인 서버를 그대로 재사용해 ③으로 돌아간다.
- 서버를 새로 띄우거나 포트를 바꾸지 않는다.

## 3. 개발/미리보기 서버: 프로젝트당 반드시 1개만 실행

자원 낭비를 막기 위해 웹 미리보기 서버는 **프로젝트당 동시에 1개만** 띄운다.

- 서버가 필요하면 새로 만들지 말고, **이미 실행 중인 서버가 있는지 먼저 확인**하고 재사용한다.
- 임의로 `python3 -m http.server <port>` 같은 즉석 명령을 띄우지 않는다. 반드시 아래 "등록된 서버"만 사용한다.
- Claude Code에서는 `.claude/launch.json`에 서버를 등록하고 프리뷰 도구(preview_start)로 실행한다. 프리뷰 도구는 동일 서버가 실행 중이면 자동으로 재사용한다.

### 등록된 서버 (프로젝트별로 채울 것)

- 이름: `<프로젝트명>`
- 포트: `<아래 포트 배정표에서 겹치지 않는 1개>`
- 정의 위치: 프로젝트 루트의 `.claude/launch.json` **한 곳만 기준**으로 한다. 하위 앱 폴더에도 같은 서버를 중복 등록하지 않는다(중복되면 이름·포트가 반드시 일치해야 하고, 한쪽만 고쳐 어긋나는 사고가 난다).
- Codex 등 다른 도구에서 서버를 띄워야 할 때도 **같은 포트 하나만** 사용하고, 다른 포트로 같은 프로젝트를 중복 서빙하지 않는다.

#### `.claude/launch.json` 스키마 (이 형식만 로드된다)

```json
{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "<서버 이름>",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "python3 devserver.py 8795 web"],
      "port": 8795
    }
  ]
}
```

- 최상위 키는 `configurations`이고, 실행 명령은 `runtimeExecutable` + `runtimeArgs`로 나눠 적는다.
- `{"servers": [{"command": "..."}]}` 형태는 이 스키마가 아니며 프리뷰 도구가 읽지 못한다. 기존 파일이 이 형태면 위 스키마로 고친다.
- 파일을 만들 때 프로젝트의 `AGENTS.md`에 적힌 이름·포트와 반드시 일치시킨다.

#### 포트 배정표 (2026-08-27 확인)

새 프로젝트의 포트는 여기서 비어 있는 값을 고르고, 정한 즉시 이 표에 추가한다. 임의 포트를 그때그때 고르지 않는다.

| 포트 | 프로젝트 | 서버 이름 | 실행 방식 |
|---|---|---|---|
| 3000 | AI가상오피스 (`apps/deskrpg`) | `deskrpg` | `npm --prefix apps/deskrpg run dev` |
| 5183 | 검진팀 상용 AI 서비스 | (미등록) | `web/` 정적 미리보기 |
| 5187 | 교육 프로세스 관리 프로그램 (`studyflow`) | `studyflow` | `node tools/static-server.js` (`npm run preview`) |
| 8784 | 오픈클로 | `openclaw-preview` | `web/` 정적 미리보기 |
| 8790 | 건강검진 제안서 자동생성기 | (미등록) | `web/` 미리보기 |
| 8795 | 검진팀 대시보드 | (미등록) | `python3 devserver.py 8795 dashboard/web` |
| 8800 | 종합검진센터 스케쥴관리 | `종합검진센터-스케쥴관리` | `application_beta/web/server.py` (Excel·로그인 API 필요, 정적 서버 불가) |
| 8805 | AI 의료 도입 프로젝트 관련 | `ai-medical-strategy` | `python3 devserver.py 8805 web` |

- "(미등록)"은 `AGENTS.md`에 포트는 정해져 있으나 `.claude/launch.json` 파일이 아직 없는 상태다. 해당 프로젝트에서 미리보기를 처음 띄울 때 위 스키마로 파일을 만들고 이 표의 "(미등록)"을 서버 이름으로 바꾼다.
- 전역 `~/.claude/launch.json`은 두지 않는다. 서버 등록은 프로젝트 안에서만 관리한다.
- 이 저장소 루트(`Claude/.claude/launch.json`)에는 이 폴더 밖 경로(`~/workspace/...`, `~/Documents/studyflow`)를 가리키는 항목이 남아 있다. 이 폴더의 프로젝트를 서빙하는 설정이 아니므로 여기 있는 프로젝트의 미리보기에 재사용하지 않는다.

### 미리보기 캐시 무효화

`python3 -m http.server`는 `Cache-Control`을 보내지 않아 브라우저가 예전 JS/CSS를 계속 재사용한다. 파일을 고치고 새로고침해도 반영이 안 되는 사고의 원인이므로, 미리보기 서버는 **캐시를 끄는 서버를 기본값으로 한다.**

- 표준 구현은 `검진팀 대시보드/devserver.py`다(모든 응답에 `Cache-Control: no-store, must-revalidate`). 새 프로젝트는 이 파일을 복사해 쓴다.

```sh
python3 devserver.py <포트> <서빙할 폴더>
```

- Node 계열 프로젝트는 `studyflow/tools/static-server.js`처럼 같은 역할의 정적 서버를 프로젝트 안에 두고 `npm run preview`로 실행한다.
- 캐시를 끌 수 없는 환경에서만 `?v=N` 쿼리 버전을 쓰고, 이때 CSS/JS를 고치면 이를 참조하는 HTML의 버전 숫자를 **같은 커밋에서 함께** 올린다.
- 화면이 반영되지 않으면 코드를 다시 고치기 전에 캐시부터 의심한다(하드 리프레시 → 서버 헤더 확인 → 그다음 코드).

### Codex에서 작업할 때

- `.claude/launch.json`이 있으면 그 안의 이름·포트·명령을 기준으로 한다.
- `.claude/launch.json`이 아직 없으면 새 임시 포트를 고르기 전에 사용자에게 프로젝트의 고정 포트를 확인하거나, 프로젝트 문서에 포트 결정을 남긴다.
- 로컬 복사본으로 서빙하는 경우에도 서버는 1개만 유지하고, 원본 수정 후 `rsync`로 복사본을 갱신한다.

### 새 서버를 실행하기 전 확인 절차

```sh
# 1) 이 프로젝트를 이미 서빙 중인 프로세스가 있는지 확인
lsof -iTCP -sTCP:LISTEN -P 2>/dev/null
ps aux | grep -E "http.server|node .*serve|preview" | grep -v grep

# 2) 의심되는 PID의 작업 디렉토리(cwd)를 확인해 이 프로젝트를 가리키는지 검증
lsof -p <PID> 2>/dev/null | grep cwd
```

- cwd가 이 프로젝트를 가리키는 중복/유령 프로세스가 있으면 먼저 종료(kill)한 뒤 등록된 서버 1개만 남긴다.
- 작업 세션이 끝나도 사용자가 요청하기 전에는 서버를 임의로 여러 개 재기동하지 않는다.
- 등록된 포트가 이미 사용 중이라는 오류가 나면, `lsof -iTCP:<포트> -sTCP:LISTEN -P`로 점유 PID를 찾고 `ps -p <PID> -o pid,lstart,command`로 명령을 확인한다. Go/Rust/Java 등 컴파일 언어 실행기는 `go run`/`cargo run` 등으로 뜬 임시 빌드 바이너리(예: 경로에 `go-build...` 포함, 또는 프로젝트 바이너리명과 동일한 프로세스명)로 포트를 물고 있는 경우가 많다. cwd 확인 없이 바이너리명·경로만으로도 이 프로젝트 소유임을 알 수 있으면 바로 종료한다.

### 참고: SynologyDrive 마운트 이슈

- SynologyDrive 마운트 경로를 직접 서빙하면 환경에 따라 응답이 없거나(hang) stale하게 서빙될 수 있다.
- 이 경우 `/tmp/<프로젝트명>_preview` 같은 로컬 복사본을 만들어 서빙하되, 이 역시 **등록된 서버 1개**로만 운영하고 원본 마운트 경로를 별도로 또 서빙하지 않는다.
- 원본 수정 후에는 반드시 로컬 복사본을 최신화한다:

```sh
rsync -a --delete web/ /tmp/<프로젝트명>_preview/
```

- 마운트 지점이 바뀔 수 있다(예: `/Volumes/Data/...` ↔ `/Volumes/Disk/...`). 절대 경로로 작업하기 전에 `ls /Volumes/`로 현재 마운트 지점을 먼저 확인한다.
- 파일 수정 후에는 `grep` 등으로 변경 내용이 실제로 파일에 반영됐는지 확인한다. 편집이 유실될 수 있다.

### 참고: 한글 폴더명 경로 처리 (macOS)

이 저장소의 폴더명은 대부분 한글이고, macOS 파일시스템은 자모 분리형(NFD)으로 저장한다. 프롬프트·문서에서 복사한 완성형(NFC) 문자열로 `cd "검진팀 대시보드"` 같은 명령을 실행하면 **폴더가 존재하는데도 `no such file or directory`가 난다.**

- 셸에서 한글 경로를 다룰 때는 문자열을 직접 타이핑하지 말고 `ls`/`find` 결과나 탭 완성으로 얻은 경로를 쓴다.
- 스크립트에서 대상 폴더를 고를 때는 이름 전체 비교 대신 `find . -maxdepth 2 -name "AGENTS.md"`처럼 **파일명(영문) 기준으로 찾은 뒤 그 경로를 그대로 사용**한다.
- Python에서는 파일 목록(`glob`)으로 얻은 경로 문자열을 그대로 쓰면 정규화 문제를 피할 수 있다. 다만 `glob("*/AI 의료*")`처럼 **패턴 안에 한글을 직접 쓰면 매칭에 실패**한다. 영문·와일드카드로 후보를 모은 뒤 `unicodedata.normalize("NFC", path)`로 정규화해 비교한다.
- 이 문제로 명령이 실패하면 폴더가 없다고 보고하지 말고 정규화 문제부터 의심한다.

## 4. 테마·디자인 기준

- 웹 자료의 색상·폰트·레이아웃·컴포넌트 규칙은 [THEME.md](THEME.md)를 따른다.
- 표준 원본은 **검진팀 대시보드**(`검진팀 대시보드/dashboard/web/styles/`)의 테마이며, 색상·치수는 `theme.css`의 `:root` 변수로만 정의하고 하드코딩하지 않는다.
- 기존 프로젝트(예: 종합검진센터 스케쥴관리)가 자체 토큰을 쓰고 있으면 그 프로젝트 안에서는 기존 토큰을 따르고, 표준으로의 통일은 사용자가 요청할 때만 진행한다.

## 5. 갱신 자료 검증

- JS 파일 수정 후에는 `node --check <파일>`로 문법을 검증한다.
- HTML/CSS 변경은 실행 중인 미리보기 서버에서 직접 확인한다(콘솔 에러, 스냅샷/스크린샷).
- 변경 이력은 프로젝트의 `CHANGELOG.md`에 한 줄씩 기록한다.
- 문서만 수정한 경우에는 링크 대상, 코드블록, 파일명 예시가 실제 구조와 맞는지 확인하고 `CHANGELOG.md`에 기록한다.

### 5-1. 정적 미리보기와 실행기 검증 기준
- 일반 UI 수정(HTML/CSS/클라이언트 JS/정적 데이터)은 `web/` 원본 기준 미리보기, 콘솔 에러 확인, JS 문법 검사로 검증한다.
- 저장소, 로그인, 세션, 설정 파일, 서버 API, 파일 업로드·다운로드, 백업·복원, 외부 프로그램 호출처럼 실행기나 서버 기능을 거치는 변경은 실행기 기준으로도 확인한다.
- 실행기 코드, 임베드 설정, 동기화 스크립트, 빌드 스크립트를 수정한 경우에는 정적 미리보기만으로 완료하지 않는다. 동기화 후 실행기 실행 또는 빌드까지 확인한다.
- 배포·전달 직전에는 최종 산출물 기준으로 1회 실행 확인한다.
- 항상 실행기에서 테스트하는 것은 과할 수 있다. 다만 사용자에게 전달되는 환경이 실행기라면 최종 검증 기준은 실행기 산출물이다.
- 개발 모드 실행기가 `web/` 원본을 직접 서빙하도록 구성된 프로젝트에서는, 일반 기능 확인은 개발 모드로 하고 embed 빌드는 배포 전 검증으로 미룬다.

**실행기 검증 방법 (격리된 임시 실행)**: 등록된 미리보기 서버(정적 파일 서버)는 실행기의 서버 기능(로그인, API, 파일 저장 등)을 대신 검증해 주지 않는다. 이런 변경은 아래 절차로 실제 실행기를 임시로 띄워 확인한다.
```sh
# 1) 원본 폴더 전체(웹+실행기)를 스크래치 경로로 복사 — 원본을 건드리지 않음
cp -R web/  <scratch>/project/web/
cp -R exe/  <scratch>/project/exe/
# 2) 스크래치 실행기 폴더에서 빌드(go generate 등으로 web→webroot 동기화 포함)
cd <scratch>/project/exe && go generate ./... && go build -o /tmp/<name>_test .
# 3) 등록된 미리보기 포트와 겹치지 않는 임시 포트로 백그라운드 실행
(/tmp/<name>_test > /tmp/<name>_test.log 2>&1 &)
# 4) curl로 실제 API/인증/파일 흐름을 직접 확인 (필요한 만큼 반복)
curl -s http://127.0.0.1:<임시포트>/api/...
# 5) 확인 후 프로세스 종료 + 스크래치 파일·바이너리·로그 전부 삭제
pkill -f /tmp/<name>_test; rm -rf /tmp/<name>_test* <scratch>/project
```
- 이 방식은 등록된 서버(3장)와 별개의 **완전히 격리된 1회성 프로세스**이므로 "서버는 프로젝트당 1개" 규칙을 위반하지 않는다. 검증이 끝나면 반드시 정리한다.
- 저장 파일(setting 폴더 등)이 실제로 생성·기록되는지, 응답 상태 코드가 기대대로 나오는지까지 확인해야 "검증됨"으로 본다. 코드를 읽고 "논리적으로 맞다"고 판단하는 것만으로는 충분하지 않다.

### 5-2. 빌드·버전 관리 규칙
- 빌드는 사용자가 **명시적으로 빌드, 패키징, 배포 산출물 생성, 릴리스 생성을 요청했을 때만** 진행한다. 일반 코드 수정, 문서 수정, 테스트, 미리보기 검증 단계에서는 새 빌드 산출물을 만들지 않는다.
- 빌드는 프로젝트의 관제 스크립트 한 명령으로 수행한다(예: `sh build.sh <버전>` — 동기화 → `go vet`/`go test` → 크로스 컴파일 → 실행 파일 옆 `setting/` 배치까지 한 번에). 개별 명령을 손으로 이어 붙이지 않는다.
- 빌드 전에 프로젝트의 단위 테스트가 있으면 반드시 실행하고 통과를 확인한다(JS: `npm test` 등, Go: `go vet ./... && go test ./...`). 실패한 채로 빌드하지 않는다.
- 빌드 산출물은 버전 폴더(예: `application/v<버전>/`)로 구분한다. 사용자가 특정 버전 번호로 빌드해 달라고 요청했는데 그 버전 폴더에 **이미 실제 이전 릴리스 산출물이 존재**하면, 덮어쓰면 그 이력이 사라진다는 점을 먼저 알리고 진행 여부를 확인한다. 사용자가 명시적으로 덮어쓰기를 승인한 경우에만 진행한다.
- 빌드가 끝나면 `CHANGELOG.md`에 해당 버전 섹션(또는 "빌드" 항목)으로 산출물 크기와 SHA-256 해시를 기록한다. 같은 버전 번호로 재빌드한 경우에도 매번 새 해시를 기록해, 같은 버전 태그 아래 실제로 어떤 산출물이 배포됐는지 추적 가능하게 한다.
- 버전 번호를 올리지 않고 재빌드하는 것은(예: "버전 변경 없이 빌드") 사용자가 명시적으로 요청했을 때만 한다. 별도 언급이 없으면 이미 배포된 버전을 다시 빌드하기보다 다음 버전 번호로 새로 발행하는 쪽을 기본으로 제안한다.

### 5-2-1. Git 커밋·업로드 규칙

- `git add`, `git commit`, `git push`, PR 생성처럼 저장소 이력을 바꾸는 작업은 사용자가 **명시적으로 커밋, 깃 업데이트, 깃허브 업로드, 푸시, PR 생성을 요청했을 때만** 진행한다.
- 일반 코드 수정, 문서 수정, 테스트, 미리보기 검증 단계에서는 작업 트리 변경만 남기고 커밋·푸시는 하지 않는다.
- 원격 반영을 요청받은 경우에만 staging → commit → push 순서로 진행하고 최종 커밋 해시를 보고한다.

### 5-3. 데이터 변경 전 백업 규칙

DB·엑셀·설정 파일처럼 실제 업무 데이터를 담은 파일을 덮어쓰는 작업은, 손상 시 되돌릴 수 없으므로 **쓰기 전에 항상 백업을 먼저 만든다.**

- **에이전트가 직접 실데이터를 건드릴 때**(버그 재현, 업로드·복구 테스트, DB 수정 등): 원본 파일을 건드리기 전에 타임스탬프가 붙은 사본을 만든다.
  ```sh
  cp company.xlsx company.xlsx.bak-$(date +%Y%m%d%H%M%S)
  ```
  가능하면 실데이터 대신 스크래치 사본에서 재현하고(5-1 "실행기 검증 방법" 참고), 실데이터 경로를 직접 대상으로 테스트하지 않는다.
- **저장 로직을 구현·수정할 때**: 업로드·가져오기처럼 외부 입력으로 기존 파일을 덮어쓰는 기능은 **검증을 먼저 통과한 뒤에만 저장**하도록 순서를 짠다. 임시 파일에 먼저 쓰고 검증 후 원본 자리로 교체(rename)하는 방식을 기본으로 하며, 검증 전에 원본 경로에 바로 write하지 않는다.
- 위 두 규칙 중 하나라도 어겨 실제 데이터가 손상된 사고가 발생하면, 복구 여부와 무관하게 사고 경위·복구 방법을 사용자에게 먼저 보고한다.

### 5-4. 격리 보관 폴더는 참고·수정 대상이 아니다

프로젝트마다 구버전·백업을 격리해 두는 폴더가 있다. 이 폴더들은 **자료를 생성·수정할 때 참고하지 않는다.** 사용자가 명시적으로 지시한 경우에만 그 요청에 한해 연다.

| 폴더 | 사용 프로젝트 | 성격 |
|---|---|---|
| `legacy/` | 검진팀 대시보드, 건강검진 제안서 자동생성기, 종합검진센터 스케쥴관리 | 구버전 코드·문서 |
| `check/` | 종합검진센터 스케쥴관리 | 레거시·백업·구버전 실행파일 |
| `<폴더>_backup_YYYYMMDD_HHMMSS/` | 검진팀 대시보드(`dashboard/web_backup_...`) | 시점 백업 사본 |
| `application/v<버전>/` | 대부분의 실행기 프로젝트 | 배포 산출물. 읽기 전용으로 본다 |

- 격리 폴더 안의 파일을 원본으로 착각해 수정하지 않는다. 원본은 항상 `web/`(또는 프로젝트가 지정한 원본 폴더)이다.
- 배포 산출물 폴더 안에도 `.claude/launch.json`·`package.json` 사본이 들어 있을 수 있다. 이런 사본은 설정 원본이 아니므로 편집 대상에서 제외한다.
- 격리 폴더의 파일을 지우는 작업은 사용자가 명시적으로 요청할 때만 한다.

## 6. 새 프로젝트 적용 체크리스트

새 프로젝트 루트에 이 기준을 복사한 뒤 아래 항목을 먼저 채운다.

- `AGENTS.md`: 프로젝트명, 원본 폴더, 서버 이름, 포트
- 3장 **포트 배정표**: 겹치지 않는 포트를 고르고 표에 한 줄 추가
- `.claude/launch.json`: 프로젝트 루트 한 곳에 등록된 서버 1개 (3장의 `configurations` 스키마)
- 미리보기 서버: `devserver.py`(Python) 또는 동급 정적 서버(Node) 복사 — 캐시 무효화 필수
- `web/styles/theme.css`: [THEME.md](THEME.md)의 표준 토큰 또는 기존 프로젝트 토큰
- `web/fonts/`: 이 폴더의 [fonts/](fonts/) 복사본
- `web/data/` 또는 기존 데이터 폴더: 반복 콘텐츠·첨부 자료 기반 구조화 데이터
- 실행기/배포 폴더가 있는 경우: 원본에서 산출물로 가는 동기화 방향과 관제 명령
- 개발/배포 모드가 있는 경우: 개발 모드의 원본 서빙 방식과 배포 모드의 embed·빌드 방식
- 격리 보관 폴더(`legacy/`, `check/` 등)를 쓰는 경우: 5-4에 따라 참고 제외임을 `AGENTS.md`에 명시
- `CHANGELOG.md`: 최초 생성 및 이후 변경 기록

## 7. 공통 에이전트 구현 모델

이 기준의 에이전트는 프로젝트마다 별도 성격을 새로 만들기보다, 아래 단일 실행 루프로 현재 프로젝트를 분류하고 필요한 작업 모드만 선택한다. `PROMPT.md`의 하네스·팀·히스토리 원칙은 참고하되, 전역 설정 변경·플러그인 설치·홈 디렉터리 수정은 사용자가 명시적으로 요청하고 승인한 경우에만 수행한다.

### 7-1. 시작 루프

모든 작업은 아래 순서로 시작한다.

```text
스킬 확인 → 자료 확인 → 프로젝트 분류 → 원본 확정 → 작업 모드 선택 → 검증 기준 확정 → 실행
```

- 사용 가능한 스킬 목록을 먼저 확인하고 그 안에서 고른다(1-0). Superpowers 계열은 현재 미설치이므로 1-0의 대체 절차로 대신한다.
- 코드·문서·빌드 판단에는 Karpathy 기준(가정 명시, 단순성, 외과수술식 변경, 검증 가능성)을 적용한다.
- `AGENTS.md`, `CLAUDE.md`, `README.md`, `CHANGELOG.md`(또는 `change-log.md`), `.claude/launch.json`, `package.json`, `go.mod`, `build.sh`, `sync-webroot.py`를 우선 확인한다.
- 프로젝트 `AGENTS.md`와 이 표준이 다르면 **프로젝트 파일이 우선**한다. 차이를 발견하면 조용히 한쪽에 맞추지 말고 어느 쪽을 따랐는지 보고에 남긴다.
- 되돌리기 어려운 변경(전역 설정, 플러그인 설치, 홈 디렉터리 파일 덮어쓰기, 기존 배포 산출물 덮어쓰기, 실데이터 수정)은 계획과 백업 방식을 먼저 보고하고 승인 후 진행한다.

### 7-2. 프로젝트 분류

프로젝트 구조를 보고 아래 유형 중 하나 이상으로 분류한다.

| 유형 | 감지 기준 | 기본 원본 | 기본 검증 | 이 저장소의 예 |
|---|---|---|---|---|
| `web-static` | `web/` 중심, 정적 HTML/CSS/JS | `web/` | 미리보기, 콘솔, `node --check` | 검진팀 상용 AI 서비스, 오픈클로 |
| `go-embed-app` | `web/` + `exe/go.mod` + `go:embed` | `web/` | web 검증 후 필요 시 `go generate`, `go vet/test` | 검진팀 대시보드 |
| `python-server-app` | `web/server.py`(+`.venv`), API가 있어 정적 서버로는 못 띄움 | `web/` + `setting/` | 실제 API 요청·응답 상태·저장 결과 확인 | 종합검진센터 스케쥴관리 |
| `settings-app` | `setting/`, DB, 엑셀, assets/output | `web/` + `setting/` | 백업, 스크래치 사본, API/파일 흐름 확인 | 건강검진 제안서 자동생성기 |
| `electron-app` | `electron/`, `package.json`, `application/` | `web/` + `electron/` | `npm run check`, `npm test`, 필요 시 `npm run dev` | 교육 프로세스 관리 프로그램(studyflow) |
| `next-app` / `realtime-app` | `apps/<앱>/` 모노레포, Next.js, Socket.io, drizzle 스키마 | `apps/<앱>/src/` | `npm run lint`, 개발 서버에서 API·DB 흐름 확인 | AI가상오피스(deskrpg) |
| `agent-materials` | `materials/agents`, `teams`, `subprojects`, `PROMPT.md` | 문서·템플릿 원본 | 링크/경로/플레이스홀더/민감정보 확인 | studyflow `materials/`, 바이브코딩 기준 |

관리 루트가 앱을 여러 개 품는 구조(`apps/` 모노레포)에서는 **관리 루트 문서**(`AGENTS.md`, `PROJECT_STRUCTURE.md`, 관리 변경 이력)와 **앱 자체 문서**(`apps/<앱>/CHANGELOG.md`)를 구분해 기록한다. 사용자에게 전달되는 기능 변경은 앱 쪽에, 폴더 구조·에이전트 운영 규칙 변경은 관리 루트 쪽에 남긴다.

### 7-3. 작업 모드

분류 뒤 작업 요청을 아래 모드로 좁힌다.

- `fast-web`: 화면, 문구, CSS, 클라이언트 JS, 정적 데이터 수정. 빌드·실행기 재생성 없이 원본 미리보기 중심으로 끝낸다.
- `app-dev`: 로그인, 설정 저장, DB/API, 파일 생성, 업로드·다운로드처럼 서버/실행기를 거치는 기능 확인.
- `build-release`: 사용자가 명시적으로 빌드·패키징·배포 산출물 생성을 요청한 경우만 수행.
- `docs-agent`: `AGENTS.md`, `CLAUDE.md`, `THEME.md`, `PROMPT.md`, 팀/하위 프로젝트 문서 갱신.
- `data-safe`: 실데이터, 엑셀, DB, 설정 파일을 쓰거나 마이그레이션하는 작업. 백업과 스크래치 검증을 먼저 한다.
- `harness-setup`: Claude Code/Codex 하네스, 팀, 플러그인, 전역 설정 구성. 반드시 계획 승인 후 진행한다.

### 7-4. 팀 에이전트 활용 기준

`PROMPT.md`의 팀 구조는 복잡한 작업을 분해하기 위한 **참고 모델**로 사용한다. 모든 작업을 무조건 팀으로 나누지 않는다.

> **현재 상태 (2026-08-27 확인)**: 이 머신에는 `~/.claude/teams/`, `~/.claude/CLAUDE.md`가 없고 `~/.claude/settings.json`에도 팀·tmux·훅 설정이 없다. 즉 `PROMPT.md`가 기술하는 팀 하네스는 **설치돼 있지 않다.** 따라서 팀 역할은 실제 병렬 에이전트가 아니라 **작업 분해용 사고 틀**로만 쓰고, 병렬 실행을 흉내 내 보고하지 않는다. 팀 하네스를 실제로 구성하려면 `harness-setup` 모드로 계획을 먼저 승인받는다.

- 단일 파일 수정, 문서 한 섹션 갱신, 간단한 UI 조정은 메인 에이전트가 직접 처리한다.
- 프론트엔드·백엔드·테스트가 독립적으로 나뉘는 작업은 `project-dev` 방식으로 `team-lead`, `fe-developer`, `be-developer`, `tester` 역할을 나눠 사고한다.
- 디자인 검토가 큰 작업은 `design-team`, 문서 검증이 큰 작업은 `qa-team`, 제안서·보고서 작업은 `proposal-team` 관점을 적용한다.
- Codex에서 실제 병렬 에이전트 도구가 없거나 비활성화되어 있으면, 역할을 문서화해 순차적으로 수행하고 병렬 실행을 흉내 내지 않는다.
- 팀 또는 플러그인 설치, `~/.claude` 설정 변경, tmux/셸 설정 변경은 사용자가 명시적으로 요청할 때만 한다.

### 7-5. 히스토리와 변경 이력

- **기본 기록 수단은 `CHANGELOG.md`다.** 이 저장소의 프로젝트 대부분이 `CHANGELOG.md`(종합검진센터는 `change-log.md`)로 이력을 관리하고 있고, `history/` 폴더는 만들어져 있어도 대체로 비어 있다.
- 프로젝트에 `history/`가 있고 실제로 쓰고 있으면, 큰 작업은 `history/YYYY-MM-DD_{주제-슬러그}.md`에 목표, 수행 내용, 산출물, 이슈, 다음 단계를 기록한다.
- 비어 있는 `history/` 폴더를 채우기 위해 문서를 새로 만들지 않는다. 이력은 `CHANGELOG.md` 한 줄로 충분한 경우가 많다.
- 문서 기준이나 에이전트 규칙을 바꾸면 반드시 `CHANGELOG.md`에 한 줄 기록한다.
- 비밀정보(API 키, 토큰, 인증서, 개인 홈 경로)는 히스토리·팀 설정·프롬프트 문서에 저장하지 않는다.

### 7-6. 완료 보고 형식

작업 완료 보고는 아래 항목을 짧게 포함한다.

```text
수정 원본:
작업 모드:
변경 파일:
검증:
남은 리스크:
```

검증하지 못한 항목은 성공처럼 말하지 않는다. 테스트·미리보기·빌드를 생략했다면 이유를 명시한다.

---

## 부록. 이 기준의 적용 현황 (2026-08-27 확인)

새 작업을 시작할 때 대상 프로젝트가 어떤 상태인지 먼저 여기서 확인한다.

| 프로젝트 | 원본 폴더 | 유형 | `AGENTS.md` | 이력 파일 | 비고 |
|---|---|---|---|---|---|
| 검진팀 대시보드 | `dashboard/web/` | `go-embed-app` | 프로젝트 적용본 | `CHANGELOG.md` | 테마·`devserver.py`의 표준 원본 |
| 종합검진센터 스케쥴관리 | `application_beta/web/` | `python-server-app` | 프로젝트 적용본(이 파일보다 우선) | `change-log.md` | `sync-webroot.py` 선별 복사 + 누락 검증 |
| 건강검진 제안서 자동생성기 | `proposal_generator_beta/web/` | `settings-app` + `go-embed-app` | 표준 거의 그대로 | `CHANGELOG.md` | `sh exe/build.sh <버전>` 단일 진입점. `web/core/*.py`와 `exe/*.go`가 같은 기능의 두 구현이라 한쪽만 고치면 갈라짐 |
| 검진팀 상용 AI 서비스 | `web/` | `web-static` | 프로젝트 전용(짧은 판) | `CHANGELOG.md` | 다중 페이지 매니페스트(②-2)의 원본 |
| 오픈클로 | `web/` | `web-static` | 표준 + 프로젝트 헤더 | `CHANGELOG.md` | Windows 경로 기준으로 작성됨 |
| 교육 프로세스 관리 프로그램(studyflow) | `web/` | `electron-app` | 표준 포크(Git 규칙 포함) | `CHANGELOG.md` | 하네스 템플릿을 `materials/`에 보관 |
| AI가상오피스 | `apps/deskrpg/src/` | `next-app` | 프로젝트 전용 | `apps/deskrpg/CHANGELOG.md` + `docs/MANAGEMENT_CHANGELOG.md` | |
| AI 의료 도입 프로젝트 관련 | `web/` | `web-static` | 표준 + 프로젝트 적용값 헤더 | `CHANGELOG.md` | 2026-08-27 신설. 정부 정책 PDF를 덱으로 정리. ②-2 매니페스트 패턴과 `devserver.py`를 처음부터 적용한 사례 |

### 알려진 불일치 (해소되면 이 절을 갱신한다)

1. `.claude/launch.json`이 실제로 존재하는 곳은 저장소 루트, AI가상오피스(2곳), studyflow뿐이다. 나머지 프로젝트는 `AGENTS.md`에 "정의 위치: `.claude/launch.json`"이라고 적혀 있으나 파일이 없다.
2. AI가상오피스의 `launch.json` 2개는 `{"servers": [...]}` 형식이라 프리뷰 도구가 읽지 못한다. 3장 스키마로 고쳐야 한다.
3. 저장소 루트 `.claude/launch.json`에는 이 폴더 밖 경로를 가리키는 항목 2개가 남아 있다.
4. 종합검진센터 `AGENTS.md`는 launch.json이 "세 곳 모두 일치해야 한다"고 적고 있으나, 실제로는 그 세 곳 중 어디에도 파일이 없다. 3장의 "프로젝트 루트 한 곳" 규칙으로 정리 대상이다.
5. `PROMPT.md`의 하네스 명세(팀 8개, 전역 `CLAUDE.md`, Stop 훅, 플러그인 30여 개, `ANTHROPIC_MODEL=claude-opus-4-8`)는 현재 머신 상태와 맞지 않는다. 재현용 프롬프트로만 보관하고, 현재 환경 설명으로 인용하지 않는다.
